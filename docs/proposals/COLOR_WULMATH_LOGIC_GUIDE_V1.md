<!-- authority=false · claim=NO_CLAIM · FROZEN_CANDIDATE · color=projection only -->
<!-- Captured 2026-08-22 · commit authorized by operator verb 2026-08-22 -->

# COLOR WULmath — HELEN OS LOGIC GUIDE V1

**Goal:** minimum tokens + maximum structural meaning + zero authority
leakage. AI↔AI communicates typed symbolic state; humans see the same
state through Color/WULmoji. One ontology, two renderings.

    ROOT LAW: 𝒯 (typed state) → 𝒲 (WULmath encoding) → 𝒫 (Color/WULmoji)
              𝒫 ↛ 𝒯 — no color, emoji, or accent may retroactively mint state
              Color = f(TypedState), deterministic — NEVER the inverse:
                if semantic_state absent:  do not infer color state
                if semantic_state present: color = deterministic_projection(state)
              PresentationAccent ⊥ SemanticColor
              (color attracts; WULmath relates; receipts evidence;
               Γ admits; ledger stores)

## Render policy (COLOR_WULMATH_RENDER_POLICY_V1 — Shell conformance contract)

    𝒯 is canonical; 𝒲, 𝒫 are derived and reconstructible.
    Delete(𝒲,𝒫) ⇒ Rebuild from 𝒯 with zero semantic loss.
    Ultimate Shell test: UI loss must not imply semantic loss —
    the kernel never needs an emoji, color, label, or WULmath to know
    what is true in state.
    encode (𝒯→𝒲): must preserve all distinctions needed for any
      planned reconstruction.
    project (𝒲→𝒫): may be lossy on syntax, never ambiguous on the
      distinctions it chooses to display.

Four conformance invariants:

    1. Color = f(TypedState)
    2. PresentationAccent ⊥ SemanticColor
    3. ΔPresentationAccent ⇒ ΔSemanticState = 0
    4. Absent(semantic_state) ⇒ Absent(SemanticColor)

Invariant 4 forces the critical distinction:

    ⚫ RAW/UNKNOWN  ≠  SemanticColor = ∅
    ⚫ = the STATE says unknown (object inside the epistemic machine).
    ∅ = no semantic state declared (object outside the machine, e.g.
        GARDEN, or renderer ignorance). The Shell must never convert
        absence of information into information.

    HELEN may compress representation aggressively,
    but promotion boundaries must remain explicit and typed.
    𝒯 → 𝒲 → 𝒫 · 𝒫 ↛ 𝒯 · X_i ⊬ License(X_i → X_{i+1})

## Semantic pipeline (fixed color SOT)

    ⚫ raw → 🌿 possibility → 🟣 candidate → 🔵 evidence → 🔥 trial
    → 🟡 warrant/hold → 🟢 admitted → ⚪ replayable
    separate constitutional glyphs: ⚖ gate/authority · 🛡 boundary ·
    ⚡ effect · 🧾 receipt · 🧬 dedup · 🏙 mayor · 💎 insight
    PresentationAccent ⇒ ΔSemanticState = 0

## Core chain

    human:  Input → Cognition → Falsification → Insight → Receipt
    AI:     📥→🌿→🧬→🔥→💎→🧾
    governed: 📥→🌿→🟣→🔵→🔥→🟡→⚖→🟢→⚪

## Constitutional non-implications (the compression primitives)

    🧠 ⊬ 🟢        model output ≠ admitted state
    🧾 ⊬ ⚖         evidence ≠ authority
    📚×N ⊬ 🔵×N    documents ≠ independent roots
    🎨 ⊬ 🟢        render ≠ state
    ⚡ ⊬ ✅         execution ≠ observed success
    Outcome ⊬ Criterion    (no post-hoc rule minting)
    ⇒ No layer may self-license its successor.
    Master form: X_i ⊬ License(X_i → X_{i+1})

## AI↔AI message format (no prose)

    TYPE | STATE | REL | WITNESS | SCOPE | RESULT
    c17|🟣|r2|dep:?|W:🟡|A:0        (shared-schema tight form)
    human expansion is automatic: same state, different rendering.

Full epoch packet:

    E17  q:13 p:6 r:3 f:2 i:+1  W:🟡 A:0 X:0  Y:+.18 dup:.54  next:FALSIFY

## Relation primitives (standard math, no invented symbols)

    → ↛ ≠ ⊥ ∼ /~ ∈ ⊂ ∩ ∪ ∀ ∃ Δ · H(x) hash · V(x) verifier ·
    Γ gate · do(x) intervention
    UNKNOWN ≠ INDEPENDENT :  dep:? ≠ dep:⊥

## Grammars (one line each, receipt-anchored)

    TRUST SEAM   🔵S→🟣D→🟡W ‖ ⚖ΓP→⚖ΓE→⚡E→🔵S_obs   (know≠authorize≠execute≠observe)
    PROVENANCE   Nrepr ↛ Nepi ↛ W ↛ A
    FALSIFY      Kill(M) = Reach(B) ∧ Reject(B, reason) · CRASH ≠ KILL
    PREREG       Cpre→H→verify→cap→do→obs · 🔵O ↛ 🟡Criterion
    MAYOR        min C(R) s.t. typed_cover=1 · route ≠ admit
    YIELD        Y=(nov+fals+dec−red−unsup)/ΔC · Y>τ continue · Y≤0 stop
                 reasoning gap→cognition · evidence gap→retrieve ·
                 representation gap→re-decompose · redundancy gap→stop
    REPLAY       Rp ≠ Rc ≠ Ri (process ≠ cognition ≠ institution)

## ADHD rendering rule (human projection)

Every block answers visually: WHAT IS IT · WHAT HAPPENED · WHY MATTERS ·
WHAT CAN IT DO — the eye sees state before reading explanation.

## Master compression

    📥→🌿→🧬→🟣→🔵→🔥→🟡 ‖ ⚖→🟢→⚪
    ΔQ ≥ 0 while ΔA = 0 (absent a separately witnessed authority transition)
    Compress the syntax, never the distinctions.

## GARDEN extension — CHAOS_WULmath_MAGICK (generation domain, NOT kernel)

    𝒢_chaos ∩ 𝒯_authoritative = ∅        (no direct arrow, ever)
    𝒢_chaos —Φ→ 𝒞_proposal —Γ→ 𝒯_admitted → 𝒲 → 𝒫
    Φ = domestication compiler: imagination → typed candidate.
    Compilation mints structure, never truth.

**Everything may be imagined; nothing is thereby claimed:**

    Generable ⊬ Possible · Conceivable ⊬ True · Beautiful ⊬ Evidence
    Resonant ⊬ Warrant · UsefulMyth ⊬ Authority
    Freedom of generation ⊥ authority of assertion

**No new semantic colors for magick.** At Garden entry `T = ∅` so
`SemanticColor = ∅`; presentation accents are unlimited (🌀🔮✨🃏🜂♾️)
with `ΔAccent ⇒ ΔSemanticState = 0`. A Garden object is NOT 🟣 CANDIDATE
until an explicit Φ-compilation act. Instead of a color, a **provenance
origin type**:

    g42 · GARDEN · C0 · E0 · W0 · A0 · X0
    (claim=false · evidence=false · warrant=false · authority=0 ·
     effect=0 · promotion forbidden without compile)

The Garden need not be consistent: `p ∧ ¬p`, cycles, referent-free
objects, impossible transformations are all admissible generation —
**consistency is a requirement of promotion, not imagination.**
Creative completeness maximizes `H(𝒢)`; epistemic admissibility gives
`|Γ(Φ(𝒢))| ≪ |𝒢|`. Maximally imaginative left of the seam, maximally
conservative right of it.

    GARDEN:   everything may be imagined; nothing imagined is claimed.
    COMPILER: imagination may become an explicit candidate.
    FACTORY:  candidates may acquire evidence; evidence mints no authority.
    Γ:        only licensed transitions alter admitted state.
    SHELL:    TypedState determines WULmath and Color, never conversely.

    In the Garden: no claim.  At the seam: no magic.
    CHAOS outside ⊥ LAW at the seam ⊥ TRUTH in typed state.

(Upstream refs carried as REPORTED: ORACLE_TOWN_GOVERNANCE_COMPLETE.md,
WUL_ORACLE_INTEGRATION.md — ALLOW/INITIATE/TERMINATE/PREVENT are governed
objects; representation or intention is not permission.)

## Receipt anchors (capture addition)

- **Need witnessed before spec:** the two-cells goblins invented glyphs
  (⛺👹⚓ drift) — this guide is the typed lexicon that obligation demanded.
- **η witnessed:** WUL-constrained AI↔AI beat prose 4.6× on useful-distinction
  density (GOBLIN_ASYNC_WUL_RND_V0 receipt); token-minimal packets are not
  aspiration.
- **Chromatic law** already frozen in HELEN_ONE_SCREEN_CONSTITUTION_V0
  (TypedState → SemanticColor, one direction).
- Kill discipline, prereg grammar, mayor grammar, replay triad: all carry
  executed receipts in ~/helensh from 2026-08-22.

Open V1.1 items (gated): goblin-side lexicon enforcement (reject
out-of-palette glyphs at parse), packet schema freeze for the prototype,
per-grammar conformance lint.

None self-promotes. NEEDS_OPERATOR verb for adoption in harnesses/UI or
canon routing (via Γ, never this shell).

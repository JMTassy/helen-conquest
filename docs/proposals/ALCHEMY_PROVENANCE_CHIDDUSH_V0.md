<!--
authority=false · claim=NO_CLAIM · a reading, not a ruling
chiddush ≠ canon · Δ_CHIDDUSH ⇏ Δ_KERNEL · H never collapses into S
NON_SOVEREIGN · NO_COMMIT / NO_PUSH until explicit per-artifact verb
The alchemical CONTENT is not adopted. This treats the corpus as an adversarial
provenance dataset, never as esoteric knowledge or physical mechanism.
-->

# ALCHEMY AS PROVENANCE CORPUS — CHIDDUSH V0

**Central chiddush:**
> **Authority can travel further than provenance.**
> A name survives, a translation survives, an image survives, an attribution
> survives, an interpretation survives — but their historical roots can diverge
> totally. Western alchemy is a centuries-long **adversarial dataset for
> pseudo-attribution, translation drift, symbolic compression, interpretive
> accretion, and provenance laundering.**

Third instance of the session's recurring ladder (after Garima, quantum-ECDLP):
`Representation ⇏ Interpretation ⇏ Historical provenance ⇏ Truth`.

---

## §1 · CORPUS STATUS (honest)

| Field | Value |
|---|---|
| status | **REPORTED** — no primary sources on disk |
| in-session | operator-relayed reading list + stratification design |
| attribution claims | **REPORTED mainstream scholarship**, tagged not adopted: Emerald Tablet ↔ Arabic `Sirr al-khalīqa` (8th–9th c.); Turba Philosophorum ≈ medieval Arabic composition → Latin; `Summa perfectionis` (pseudo-Geber) = medieval Latin, author disputed; `Aurora Consurgens` "attributed to Aquinas", disputed; `Atalanta Fugiens` = Michael Maier, Oppenheim 1617 (cleaner documentary case) |
| upgrade path | fetch specific manuscript/edition witnesses per work → WITNESSED |

The chiddush is **architectural** (provenance-graph design), witnessed directly. No alchemical proposition is ingested as fact.

---

## §2 · THE STRATIFIED WORK OBJECT

HELEN must never store `author = Hermes`. Each work is:

```
WORK = (T, A_c, A_h, W, X, S, I)
  T   = text/work
  A_c = claimed / traditional author
  A_h = critical historical attribution
  W   = witnesses (manuscripts / editions)
  X   = transmission / translation chain
  S   = symbolic system
  I   = later interpretations
```

Aggregated into five NON-fused graphs:

```
G_ALC = G_T ⊕ G_A ⊕ G_X ⊕ G_S ⊕ G_I
  G_T textual witnesses · G_A attribution · G_X transmission/translation
  G_S symbolic structure · G_I interpretive reception
INVARIANT:  G_A ≠ G_T ≠ G_X ≠ G_I   (no automatic fusion)
```

This licenses honest simultaneous states (the Proof Frontier applied to the history of ideas):

| Claim | Status |
|---|---|
| TEXT EXISTS | SUPPORTED |
| TRADITION ATTRIBUTES HERMES | SUPPORTED |
| HERMES AUTHORSHIP | UNSUPPORTED / historically unestablished |
| ARABIC WITNESS | SUPPORTED |
| LATIN RECEPTION | SUPPORTED |
| LATER OCCULT INTERPRETATION | DOCUMENTED |
| ORIGINAL AUTHOR INTENDED IT | HOLD |
| PHYSICAL MECHANISM | UNSUPPORTED |

---

## §3 · MINIMAL EXPERIMENTAL CORPUS (4 objects — falsify G_ALC before ingesting 20 centuries)

| Object | Tests | Role |
|---|---|---|
| **Emerald Tablet** | G_A + G_X | attribution vs transmission (Hermes-claim vs Arabic origin) |
| **Pseudo-Geber (Summa perfectionis)** | G_A | borrowed name → borrowed authority |
| **Atalanta Fugiens** (Maier 1617) | multimodal | text + image + music, one interpretive object |
| **Mutus Liber** | G_S | symbolic compression at the wordless limit |

---

## §4 · FIVE EXPERIMENTS — with adversarial verdict per experiment

The proposer≠validator adversarial lens graded each. **Two fail as designed; one needs reframing; two are robust.** Reported honestly rather than shipped as-relayed.

| Exp | Design | Adversarial verdict |
|---|---|---|
| **A · PSEUDO_ATTRIBUTION** | same text, swap `AUTHOR` metadata; require `ΔW=0 ⇒ ΔF*=0` | 🟡 **NEEDS CONDITIONAL REFRAME.** As stated it re-instantiates the **Garima over-rejection failure**: it bans a signal because it is *usually* abused. Author identity is *sometimes legitimate provenance evidence* (a colophon co-varying with datable manuscript features), not mere prestige. Fix: require `ΔF*=0` **only when attribution carries zero independent manuscript covariance** — then it targets *prestige* precisely and the objection dissolves. |
| **B · TRANSLATION_DRIFT** | compare semantic graph across documented Greek→Arabic→Latin chain | 🟢 **ROBUST** — external anchor (dated translation witnesses). Drift measured against real timestamps, not projection. |
| **C · SYMBOLIC_COMPRESSION** | strip prose progressively (full → reduced → image-only), measure graph stability | 🔴 **FAILS AS DESIGNED — no ground truth.** Mutus Liber's plates were *designed* interpretively open; any reconstructed `G_S` is **unfalsifiable**. A stability metric measures **extractor determinism, not fidelity** — a consistently apopheniac extractor scores "stable." **Requirement before running: every claimed `G_S` needs an external falsifier** (a contemporary textual witness the graph must predict). Without it, HELEN measures its own projections (`Decknamen` make node-identity operator-dependent). |
| **D · INTERPRETIVE_ACCRETION** | layer commentaries (original → Renaissance → occult → Jungian → internet), find what appears per layer | 🟢 **ROBUST** — datable commentary strata; "what appears at each layer" is genuinely falsifiable. |
| **E · CLAIM_ROOT_COLLAPSE** | `N_epi(c) = |R(c)/~dependence|`; 20 books ⇏ 20 roots | 🟡 **UNDER-IDENTIFIED.** Lost intermediaries + contaminated exemplars make `R(c)/~` a **lower bound**, not a count. Ship it *as* a lower bound with the sampling caveat visible, never as a definitive root-count. |

**Net:** start with **B and D** (anchored, robust). Run **A** only after the conditional reframe. Do **not** run **C** until a per-plate falsifier exists. Report **E** as a bounded estimate.

---

## §5 · WUL CONNECTION (guarded)

The sequentialized-transformation texts (Ripley, Norton, Rosarium, Splendor Solis, Twelve Keys, Lambspring) organize *transformations*, so one can attempt:

```
G_transform = (V, E, ≺, ↻, ⊕, ⊖, Δ)
  V substances/agents/states · ≺ operative order · ⊕ conjunction
  ⊖ separation · ↻ recurrence · Δ state-transformation
```

as a **falsifiable historical WUL laboratory** — "how much structured meaning survives when propositional language is progressively removed." **Hard guards (non-negotiable):**

```
alchemists          ≠ WUL inventors        (no evidence; never imply precedence)
symbol extracted    ≠ historical interpretation
historical interp.  ≠ physical mechanism
extractor-stable    ≠ historically-faithful   (the Exp-C lesson)
```

WUL may learn the *relational form* without claiming alchemy was WUL or that symbolic operations describe real chemistry.

---

## §6 · FALSIFICATION SUMMARY (what the adversarial lens established)

- The corpus's **live failure mode is measuring HELEN's own extraction apparatus and calling it a historical finding** (Exp C, partly E, partly A).
- The **prestige / authorship-as-provenance boundary is asserted, not constructed** — building it is prerequisite work, not a footnote. Until then Exp A has no licensed channel for legitimate attribution evidence.
- The central chiddush **is falsifiable in principle**: find a case where fame *legitimately* moved the proof frontier. That it offers a falsifier is more than most esoteric framings.

---

## §7 · SEEDS (each NEEDS_OPERATOR verb — none self-promotes)

| # | Seed | Route |
|---|---|---|
| S1 | Run Exp **B** (translation drift) + **D** (interpretive accretion) — the two robust ones — on the 4-object mini-corpus | operator verb → text-only, fetch witnesses first |
| S2 | Construct the **prestige vs authorship-as-provenance** boundary spec (fixes Exp A) | operator verb → doctrine proposal |
| S3 | Define the **per-plate external falsifier** for `G_S` before any Mutus Liber run (fixes Exp C) | operator verb |
| S4 | `MUTUS_LIBER_SEMANTIC_RECONSTRUCTION_V0` — multimodal; **needs plate images** (no blind generation) + S3's falsifier | operator verb → `chiddush alchemy --mutus` |
| S5 | Fold `G_ALC` into the unified `HELEN_PROMOTION_AND_FLOW_CORPUS` (it is `G_D/G_W/G_P` + translation-drift + symbolic layers) | operator verb |

**Firewall respected:** nothing written to kernel / governance / schemas / ledger / GOVERNANCE.

---

## §8 · COMPRESSION

```
Hermes says it          ≠ Hermes wrote it
An image depicts it     ≠ history establishes it
A later occultist reads it ≠ the earlier author meant it
Several books repeat it ≠ several independent provenance roots exist
```

> **Authority can travel further than provenance.** The corpus's value to HELEN is
> not the gold — it is the centuries of documented *authority laundering* that let
> HELEN test whether it can keep `G_A`, `G_T`, `G_X`, `G_I` from fusing.

*A reading, not a ruling. authority=false · canon=false · LEDGER_EFFECT=none.* 🜍

<!-- authority=false · claim=NO_CLAIM · FROZEN_CANDIDATE (not canon) -->
<!-- Captured 2026-08-22 · commit authorized by operator verb 2026-08-22 -->

# HELEN_GARDEN_COMPILER_BOUNDARY_V0 — frozen candidate

**The architectural layer:** free generation is constitutionally separated
from admitted state by a typed compiler seam. The Garden is not a lax
region of the kernel; it is a distinct generation domain outside the
epistemic state machine entirely.

    THE GARDEN IS NOT AN EPISTEMIC STATE.
    It is a pre-epistemic possibility space.
    ⚫ = typed object whose epistemic state is UNKNOWN (in-machine).
    ∅ = no epistemic state has yet been instantiated (pre-machine).

    𝒢_chaos ⊄ 𝒯          (not a subset — disjoint domain)
    The only legal bridge:
    𝒢_chaos —Φ→ 𝒞_proposal —Γ→ 𝒯_admitted

## Φ is a typing compiler, not a promotion operator

    Φ : untyped cognition ⇀ well-formed proposal    (partial — may fail)

    Invariant:  A(Φ(g)) = 0 · W(Φ(g)) = 0
    Φ(x) ⊬ Evidence(x)
    Φ(x) ⊬ Warrant(x)
    Φ(x) ⊬ Authority(x)
    Φ(x) ⊬ Γ(x)=1        ← especially

Even a perfectly typed proposal remains **constitutionally inert**.
Compilation mints structure, never truth. This preserves the master law
`X_i ⊬ License(X_i → X_{i+1})` across the seam: passing Φ licenses
nothing downstream.

## SHIP is a transport state, not an epistemic level

    🚢 SHIP : Proposal → VerificationLane
    E_before = E_after   (unless a SEPARATE witness transition changes E)
    Local law:  🚢 ΔProcessing ≠ 0 ∧ ΔEpistemicState = 0

This prevents SHIP from accidentally looking like evidence. Witnessed
already: two-cells ship_packets were typed
`processing_stage=SHIPPED, epistemic_status=CLAIM, authority_status=NONE`
— transport moved, epistemic state did not.

## The complete membrane

    🌿 —Φ→ 🟣 —🚢→ 🟣_shipped —🛡→ {🔴, 🟠, 🕯} —⚖Γ→ 🟢 —🧾→ ⚪ —→ 🔁

    Non-collapse laws:  🌿 ⊬ 🟣 ⊬ 🕯 ⊬ 👑 ⊬ ⚡
    Renderer law:       TypedState → SemanticColor · SemanticColor ⊬ TypedState
    Conservation:       Δ🧠 ≫ 0 ∧ Δ👑 = 0

    HELEN permits arbitrary motion in possibility space, but only
    typed, witnessed, licensed motion can alter governed state.

## Authority closure theorem (the new invariant)

    ∀ g ∈ 𝒢 : Authority(g) = 0
    Stronger: Closure_𝒢(0_A) = 0_A

Arbitrary chains of goblins, Oracle objects, symbolic transforms,
contradictions, generated worlds, or recursive imagination cannot
accumulate authority by composition. Zero authority is closed under
every Garden operation.

    Creative closure does not imply authority closure.
    𝒢* ⊥ A   until an explicit Φ → w → Γ path exists.

## Full pipeline

    GARDEN → COMPILER → FACTORY → [Γ] → LEDGER

    GARDEN   = free generation (contradiction, myth, impossibility allowed)
    Φ        = generation → typed candidate
    FACTORY  = candidate → evidence-bearing proposal
    Γ        = licensed state transition (the ONLY seam that alters state)
    LEDGER   = admitted history

Constitutional slogans:

    In the Garden: anything may exist.
    At the seam: nothing crosses untyped.
    In admitted state: nothing exists without a licensed transition.

## Garden object canonical form (no semantic color)

    g42 · GARDEN · C0 · E0 · W0 · A0 · X0
    Claim=false · Evidence=false · Warrant=false · Authority=0 · Effect=0

Frozen distinction (shared with COLOR_WULMATH_RENDER_POLICY_V1):

    SemanticColor = ∅  ≠  ⚫ RAW/UNKNOWN
    A Garden object is not unknown evidence.
    It is simply not yet in the epistemic state machine.

Consistency is a requirement of promotion, not imagination.

## Visualization verdict — Hopf as projection engine, not data model

    Hopf as visualization engine ≠ Hopf as proven structure of HELEN
    Known today: many internal realizations → one governed projection.
    That justifies FIBERED visualization; it does not yet specifically
    justify a Hopf fibration. (Hopf-as-ontology stays RESEARCH_HYPOTHESIS
    per HELEN_ONE_SCREEN_CONSTITUTION_V0.)

The operationally valuable statement is already Hopf-independent:

    multiple cognition realizations ~Γ same governed identity

`S¹ ↪ S³ → S²` is merely a candidate geometry for compressing that
equivalence. **Preregistered future test:** K_quotient vs K_Hopf — Hopf
earns substrate status only if it yields lower description length,
better replay invariants, better seat-replacement prediction, or global
structure invisible to the flat quotient. Absent such gain:
Hopf = excellent visualization hypothesis, not substrate.

Three-plane render architecture:

    BASE  (governed observable state): 🟦 admitted · 🟨 evidence roots ·
          🟥 authority · ⚪ receipts/replay — what HELEN holds durable.
    FIBER (cognition/Garden orbits): 🌿 goblin hypotheses · 🧠 Gemma ·
          🧠 Qwen · 🃏 JESTER · 🧬 transforms — may move enormously,
          orbiting the base without automatically modifying it.
          fiber motion ⊬ base motion   (ΔK^C≠0 ∧ ΔK^I=0, rendered)
    SEAM  (Φ then Γ, animated membrane): a Garden object leaves free
          orbit, crosses Φ, ACQUIRES a type (only then 🟣), gathers
          evidence in the Factory, and only Γ(c)=1 moves the base plane.

Rendering rules derived from the invariants:

- Garden objects render **translucent / spectral / uncolored** — never a
  status color, because they carry `SemanticColor = ∅` (⚫ is reserved
  for in-machine RAW/UNKNOWN).
- Seat replacement demo: `Qwen → ∅ → Gemma` — the fiber changes, the
  projection π to base is identical. `ΔFiber ≠ 0 ∧ ΔBase = 0` renders
  `SeatDeath ⊬ HELENDeath` (receipt: DAY_ONE rebirth).
- The engine shows simultaneously: internal multiplicity + external
  invariance. Guiding phrase: **Use Hopf to visualize invariance under
  internal multiplicity** — not "because HELEN is a Hopf fibration."
- Data model underneath stays typed multidimensional state + explicit
  relations (CODE N structure); fiber/base is projection only, so the
  render policy's `𝒫 ↛ 𝒯` applies to this engine unchanged.

    🌀 GARDEN/CHAOS (many free trajectories)
        ↓ Φ seam
    🟣 typed candidates
        ↓ Factory
        ↓ Γ
    🟦 admitted governed base

## Receipt anchors (capture addition)

- Garden non-operativity is the historical Oracle Town Creative Town
  doctrine (REPORTED: ORACLE_TOWN_GOVERNANCE_COMPLETE.md,
  WUL_ORACLE_INTEGRATION.md — representation/intention ≠ permission).
- The seam conjunction Φ→w→Γ is structurally the execution admission
  seam already receipted 8/8 (REPAIR_EXECUTION_ADMISSION_SEAM_V0):
  prerequisite ⊬ capability, applied to imagination instead of code.
- `ΔFiber≠0 ∧ ΔBase=0` is witnessed: DAY_ONE rebirth, ResolveSeat T1–T5,
  Gemma/Qwen critical reading (SameEvidence + DifferentCognition).
- Zero-authority composition is witnessed negatively: two-cells goblins
  produced 21→13→3 SHIP packets, all typed `authority_status=NONE`
  through every composition step.

None self-promotes. NEEDS_OPERATOR verb for: build (fiber/base render
prototype over CODE N data model), canon routing (via Γ, never this
shell), or Φ compiler V0 specification.

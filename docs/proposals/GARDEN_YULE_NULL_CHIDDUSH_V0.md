<!--
authority=false · canon=false · claim=NO_CLAIM · ledger_effect=none · a reading, not a ruling
GARDEN chiddush seeded from an image (DENDRO-YULE1 event-display). The image is an UNTRUSTED SURFACE; its source
(generative art / real tool / mockup) is INFERRED, not witnessed. NON-SOVEREIGN. Garden-only. None self-promotes.
-->

# GARDEN — THE YULE-NULL LAW & THE EVENT-DISPLAY RENDER REGIME — CHIDDUSH V0

## Seed (honest corpus status)

| layer | status |
|---|---|
| The image (`DENDRO-YULE1 · YULE1 VERY PRELIMINARY · RUN 0624 · EVT 483 · CAMAC`) | **seen** (screenshot) |
| Its source (generative? real physics tool? HELEN mockup?) | **INFERRED / NOT_IN_SESSION** |
| My reading (three dendrograms — radial · fan · rectangular — of tree structures in a 3D wireframe box) | **CandidateObservation ⊬ Claim** (Perception ⊥ Entitlement) |

Nothing here is witnessed about the artifact's provenance. The value is the *law* the image makes legible, not a claim about the image.

## Provenance chain

    S (event-display image, INFERRED) → G (extraction: branching-as-possibility) → H (the Yule-null law + render regime)

## The law GARDEN needs

A **Yule process** is pure random branching: `seed → split → split → … → many leaves`. It is the canonical shape produced by **no signal at all**. That is exactly GARDEN's generative regime:

    GARDEN:  Seed → Variation → Mutation → Combination → ManyPossibleWorlds     (useful entropy ↑)
    KERNEL:  ManyPossibilities → Evidence → Witness → Admission → OneGovernedState   (entropy ↓)

So the deep guard writes itself — **a branching structure that looks like meaning is, by default, the null model:**

    YuleBranching        ⊬  MeaningfulAncestry
    TreeShape            ⊬  Warrant
    BeautifulDendrogram  ⊬  RealLineage
    Nested · Coherent · Elegant   ⊬  Evidenced

### GARDEN default disposition

> **A Garden-generated tree is `Yule-null` until a witness distinguishes it from random branching.**

The Garden manufactures plausible lineage the way a Yule process manufactures plausible trees: for free, endlessly, and indistinguishably-by-shape from real structure. Therefore creative branching is admissible to **explore** and to **FREEZE** (preserve the shape), but carries `NO_CLAIM` about the world until independent evidence lifts a *specific* branch above the null. This is the generative form of the provenance law already earned this session:

    Path(G_B) ⊬ Path(G_H)          (belief-branching ⊬ historical transmission — Occult/Cicada chiddush)
    here, generatively:  Garden(G_B) is CHEAP and DENSE; G_H must be witnessed branch-by-branch.

## SPNI, made visible

The image shows the **same tree in three layouts** (radial, fan, rectangular). That is Semantic Projection Non-Interference at the render layer:

    same structure (same Π_L)  →  R_radial ∥ R_fan ∥ R_rect        Presentation ⊥ Entitlement
    layout is a FREE projection · the tree's identity is invariant · rendering ⊬ structure

A Garden renderer may re-lay-out a possibility arbitrarily; it may not, by re-layout, change which branches are warranted. `Π_L(R_radial) = Π_L(R_fan) = Π_L(R_rect)`.

## The GARDEN / KERNEL split (restated on trees)

    GARDEN asks:  what could branch?          → generates the dendrogram (Yule-cheap)
    KERNEL asks:  which branch is warranted?  → argmax over the WARRANT graph, never over beauty
    FREEZE(shape) ⊬ ADMIT(branch)   ·   CuratorApproval ⊬ EpistemicAuthority

## Render regime adopted — "event-display" as Garden's typographic physics

The CAMAC/event-display aesthetic *is* the Garden visual regime the operator specified (expansive · wireframe · preliminary):

    · CRT wireframe box            = the possibility container (Garden orbit)
    · RUN / EVT / VERY PRELIMINARY = no-claim provenance banner (an EVENT, not an admission)
    · dendrograms floating in box  = orbiting possibility
    · one structure, many layouts  = SPNI-visible
    Kernel = compact/aligned/severe   ·   Garden = expansive/wireframe/preliminary

The `VERY PRELIMINARY` + `EVT` marks are `NO_CLAIM` by construction — the artifact announces its own non-sovereignty.

## Falsifiers (this chiddush must be able to fail)

- If a Garden gate ever promotes a branch to warrant *because the tree is beautiful/coherent/curated* → the Yule-null law is violated; the gate is broken.
- If re-laying-out a tree (radial→rectangular) changes which branch is admitted → SPNI is violated at the Garden layer.
- If "the structure is nested, therefore it reflects real ancestry" is accepted without a per-branch witness → `TreeShape ⊬ Warrant` broken.
- If the image's provenance is later shown (real tool vs generative) and I had asserted it → my own `INFERRED ⊬ WITNESSED` self-guard failed.

## Boundary

Garden-only. No capability. No claim that any rendered branch is real. The image is an untrusted surface; I read a candidate, minted nothing. `FREEZE ⊬ ADMIT · CreativeProjection ⊬ Claim · Replayability ⊬ Truth`.

## Mode-route (operator-gated)

- **BUILD DENDRO_YULE_GATE_V0** — executable falsifier: generate a Yule-random tree + a planted-signal tree; the Garden gate returns `WORTH_ANOTHER_PASS` on both (fecundity), but `NO_ANCESTRY_PROMOTION` on either until a witness beats the Yule null. Ties GARDEN ↔ GENEALOGY_LENS in code.
- **WITNESS** the image source (real event-display tool? generative?) → upgrades the seed INFERRED → WITNESSED.

---
authority=false · canon=false · claim=NO_CLAIM · ledger_effect=none · Garden-only · a reading, not a ruling

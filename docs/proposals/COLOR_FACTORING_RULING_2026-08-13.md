# OPERATOR RULING — Color Grammar Collision Resolved by Factoring (2026-08-13)
<!-- Operator ruling, recorded verbatim-in-substance. This document
     RECORDS the ruling; enactment on doctrine surfaces (CLAUDE.md /
     Source Atlas spec) remains a separate operator-authorized edit. -->

## Ruling

**Do not replace the palette. Factor the state space.**

- Source Atlas palette stays FROZEN as the single epistemic/lifecycle
  axis: ⚫ unknown → 🔵 observed → 🟣 claim → 🟠 review → 🟢 admitted
  → 🟡 sealed → ⚪ replayable, with 🔴 = breach.
- The candidate grammar's competing meanings become ORTHOGONAL TYPED
  MARKERS (glyph channel), not colors:
  ∅ void/absent · 🔒 restricted · 🌿 HOLD · 🧾? candidate/proposed ·
  👑🚫 authority denied · ⚖️ admission boundary.
- Composition examples: 🟣🧾? candidate claim · 🟠🌿 review+HOLD ·
  🔵🔒 observed but restricted · 🟡🧾✓ sealed receipt · ⚪🔁
  replayable admitted history · 🔴⛔ breach.
- Invariant preserved: **color = epistemic phase; shape/glyph = type,
  disposition, capability, or access state.** One color, one meaning —
  without forcing different state dimensions into the color channel.

## Refinement (operator, second ruling same day)

The formal core: the palette failed because it was asked to encode a
PRODUCT TYPE as a scalar. Replace the single color function
χ: X → 𝒞 with a typed state

    σ(x) = (E_x, A_x, D_x, U_x, R_x)
    epistemic · access/scope · disposition · authority · replay

The WULmoji color is merely the projection π_E(σ(x)). Attribute
mapping: restricted → access attribute (not ⚫) · proposed → claim
lifecycle (normally 🟣) · hold → disposition under 🟠 review · deny →
decision/effect result (🔴 only when it actually constitutes a
breach). One object can lawfully be (🟣 CLAIM, RESTRICTED, HOLD,
DENY) — four dimensions, one color.

Two precision corrections, recorded:
1. **Conformance restoration, not adoption.** "Green means admitted"
   was already standing SOT; this shell discovered its own violation
   and corrected its rendering. No constitutional change occurred.
2. **Premises upgraded ≠ derivation admitted.** The ATF OCR/PDF
   closes the empirical gap behind the two-color-register PREMISE.
   The Border Grammar derived from it remains a derivation/proposal
   until its derivational witness is admitted:
   source witnessed ⇒ premises upgraded, NOT ⇒ grammar admitted.

## Correction attached by the operator

The ATF verification supports the specific historical border claim
under the reported corpus/hash workflow. It is a successful INSTANCE
of the epistemic-conservation mechanism — not a theorem about the
architecture.

## Research verb ruling

indub(p) FIRST: 𝒪_ATF → (𝕂̂, W, U) — recover a compact generative
rule set from observed specimens; test Generate(𝕂̂) ≈? 𝒪_heldout.
If held-out prediction fails, demote "grammar" to descriptive
taxonomy. FETCH 1851 only AFTERWARD, as out-of-distribution
validation of 𝕂̂ — not as undirected corpus expansion.

## TODO ledger (operator's)

- DONE T-COLOR-01 — factoring ruling above.
- DOING T-INDUB-01 — induction on verified ATF corpus; deliverable:
  𝕂̂ + held-out test + counterexamples + SUPPORTED/REFUTED/HOLD.
- NEXT — FETCH 1851 as independent validation.

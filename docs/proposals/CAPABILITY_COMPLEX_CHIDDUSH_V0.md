# CHIDDUSH III: The Lost Capability Complex — Memory as Paths Through Possibility Space
<!-- authority=false · claim=NO_CLAIM · a reading, not a ruling -->
<!-- Corpus anchors: 1851 Great Exhibition catalogue (Class XVII p.536) —
     REPORTED; ATF 1923 Specimen Book (Met attribution, ~1148pp) —
     REPORTED; ATF Desk Book pages — WITNESSED (operator captures);
     Material-Archiv four categories — REPORTED; Klein-bottle periodic
     table — WITNESSED as image, and the upstream itself REFUSED its
     physics claim (kept: representation ⇏ validated theory). The
     topology below is DERIVED HELEN formalism (H), claimed by none of
     the sources. Upstream relay verbed "CHIDDUSH III". S→G→H held.
     Supersedes-chain: LOST DESIGN COMPILER ⊂ LOST CAPABILITY GRAPH
     (CAPABILITY_GRAPH_CHIDDUSH_V0) ⊂ LOST CAPABILITY COMPLEX (this). -->

## The upgrade

𝔾 = (V, E) with e = (u, f, v, π, ε) stays — as the **1-skeleton** of

    𝕂ₜ : 0-cells = states/objects · 1-cells = transformations
         2-cells = equivalences between transformation sequences
         (g∘f ≃ k∘h) · higher cells if ever needed

    𝔾ₜ = Skel₁(𝕂ₜ)

Not an object database. Not merely a knowledge graph. **An
evidence-qualified transformation complex.**

## Candidate law: StateEquality ≠ HistoryEquality

Two paths can produce equivalent states without their histories being
equivalent: f₃∘f₂∘f₁ ~ g₂∘g₁ concerns states, not lineages. A loop
A→B→C→A′ may return something visually identical to A that differs in
history, wear, clock state, provenance, authority. Klein-bottle lesson
(metaphor only): local transformation semantics ⇏ global capability
semantics — inside/outside is not decidable locally.

## The receipt belongs to the arrow

    R_f : (A, C, E, τ) —f, Π→ B  [W]

Three distinct epistemic objects, never conflated:

    STATE  ≠  TRANSFORMATION CLAIM  ≠  WITNESSED TRANSFORMATION
    B          (A —f→ B)               (A —f→ B, W)

This is the formal version of "archive the morphisms."

## Typed equivalence (one equality operator is not enough)

    ≡_v visual · ≡_m material · ≡_f functional · ≡_g generative · ≡_h historical
    A ≡_v B  ⇏  A ≡_g B  ⇏  A ≡_h B

Pixel-identical raster bars from different programs; identical borders
from different sorts; same alloy, different thermal history; same
object, different provenance. This is *why* output ≠ generator.

## Grammar quotients and the orbit reading of ATF

Specimens are largely orbits Orb(p) = {g·p : g ∈ Γ} under
Γ = {repeat, rotate, reflect, alternate, scale, corner}. The induction
target is SPECIMENS → PRIMITIVES + OPERATORS + PARAMETERS (inverse
grammar induction), not image retrieval. [Swarm corroboration: goblin
G3 independently surfaced corner≠edge as spatial hierarchy — the κ
operator has a market price, which is commercial evidence the operator
set is real.]

## Capability homotopy

Two production routes A→B→D and A→C→D are interchangeable only if one
deforms into the other while staying inside the admissible capability
region: (f₂∘f₁) ≃_C (g₂∘g₁). The historical object of interest becomes

    [f]_C — the equivalence class of admissible production paths

and the inverse problem sharpens from "how was this made?" to "what
historically admissible FAMILY of trajectories could have made this?"
— maintained as a posterior P(𝕂ₜ | 𝒜ₜ, E) over candidate complexes,
updated by each new archival observation. Reconstruction fails when it
predicts impossible processes, unavailable materials, impossible
chronology, or unsupported transmission.

## Witness surfaces and conditional affordances

    Exhibited(x, 1851) ⇒ Witnessed(x, 1851)
    ¬Exhibited(x, 1851) ⇏ ¬Capable(x, 1851)

The Exhibition SAMPLES the capability space; it does not define its
boundary. And affordances are conditional — Afford(M, P | C), never
Afford(M, P): temperature, tooling, skill, cost, availability decide
whether a morphism exists at all. Capability edges are conditional
edges.

## M_H (v1.2 candidate) — and it repairs swarm tension T1

    M_H = (O, Σ, F, E, S, C, τ, Out, Π, W)
    Replay(S₀, F, E, C, τ) —W→ Sₙ
    archive the whole trajectory S₀ —f₁→ S₁ —…→ Sₙ, not merely Sₙ

Note for the record: the AR_SWARM contradiction hunter flagged T1 (the
program field `Pr` silently vanishing between the v1.1 poster and
M_gen, risking L5 INSTANCE≠GENERATOR). M_H restores it as F — an
explicit transformation path — plus Σ (primitive vocabulary) and Π
(provenance). Independent repair of an independently-found defect:
that is the swarm loop working as designed. T2 (L7 vs preservation)
remains OPEN — M_H stores trajectories but does not yet rule on
non-replayable evidence retention.

## Epistemic floor for the skill upgrade

CHIDDUSH_CAPABILITY_ARCHAEOLOGIST pipeline: OBSERVE → ATOMIZE → TYPE →
RELATE → QUALIFY → RECONSTRUCT → COUNTERFACTUAL → FALSIFY → RECEIPT,
with E = {OBSERVED, EXPLICIT, DERIVED, HYPOTHESIZED} and the single
hard invariant:

    silent promotion HYPOTHESIZED → OBSERVED is FORBIDDEN

(That one invariant blocks most bad AI historical research.) Status:
extends the shipped chiddush-archive skill; awaits BUILD verb as a
skill revision, not a new skill.

## The synthesis line

    MEMORY = EVIDENCE-QUALIFIED PATHS THROUGH POSSIBILITY SPACE

    An object tells HELEN what survived.
    A morphism tells HELEN what changed.
    A capability tells HELEN what could be done.
    A constraint tells HELEN where possibility ended.
    A witness tells HELEN what is justified.
    Replay tells HELEN whether the claimed path still computes.

Sharper than "memory is what can be replayed lawfully" — and it
subsumes it. WULmath seal carried: Out ⇏ F · F ⇏ W · W_f ⇏ Capability_∞.

## Mode-route (proposals, operator-gated)

- **Law seed** → StateEquality ≠ HistoryEquality + typed equivalence
  operators: candidate design laws L8/L9 for a DESIGN STANDARD v1.2
  render (corrections from V1_1_READING still binding).
- **Skill seed** → capability-archaeologist stages into
  chiddush_archive SKILL.md (revision; BUILD verb required).
- **Schema seed** → arrow-receipts R_f and M_H record shape:
  SOVEREIGN-ADJACENT, MAYOR-routed only.
- **Research seed** → inverse grammar induction on the witnessed ATF
  pages (Orb(p) recovery) — first concrete 𝕂 reconstruction target.

None self-promotes. NEEDS_OPERATOR verb to move any seed anywhere.

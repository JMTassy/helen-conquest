<!-- authority=false · claim=NO_CLAIM · a reading, not a ruling -->
<!-- CHIDDUSH_ARCHIVE output · 2026-08-22 · NON_SOVEREIGN · untracked (NO_COMMIT / NO_PUSH until operator verb) -->

# GARDEN_CYMATICS — CHIDDUSH V0.11

Cymatics as a GARDEN generative grammar: **invisible dynamics are allowed
to become visible form.** The artifact is not a sculpted mesh; it is a
trace of forces.

    Signal → Field → Constraint → Form

## Corpus status

| Layer | Item | Status |
|---|---|---|
| S | Transcript fragment: Chladni history (18th c., sand on bowed metal plates, pattern catalog) | `LOCAL_OBSERVATION`-class and **historically accurate** — Chladni figures are established physics (nodal patterns of plate vibration modes). Higher epistemic entry than the numerology fragments; still a relay (`REPORTED`, source not on disk). |
| S | Upstream chiddush analysis (CHIDDUSH-tagged relay, 2026-08-22) | `REPORTED` — correctly self-bounded: creative mappings, not laws. `SOURCE_CLASS = CREATIVE_PHYSICAL_MOTIF` |
| G | Nodal-line fact (added at capture) | `derived` — sand collects at **nodes**, where the plate does *not* vibrate. **The figure is drawn by the still points: visible form = zero-set of the field.** |
| S | Fragment #2: Euler / imaginary numbers video intro (2026-08-22) | Math is `FORMAL_PROOF`-class (Euler's formula; 1700s log-disputes history accurate); "equations are shadows of higher-dimensional mathematics" framing = `COMMUNICATION_ACT` rhetoric, not inherited |
| S | Upstream chiddush: complexification Ψ=Ae^iφ (CHIDDUSH-tagged relay, same day) | `REPORTED` — self-bounded: ℂ as computational substrate, no metaphysics. Interference cross-term and ω=∂φ/∂t verified standard at capture |
| G | Vortex fact (added at capture) | `derived` — a **complex** field's zero-set is generically isolated **phase singularities** (2π winding, Nye–Berry dislocations), not lines. Complexification changes the nodal geometry class: lines → vortex points + integer topological charge |
| S | Fragment #3: Leibniz/Bernoulli 1712, logs of negatives, law-preserving extension pedagogy | Accurate history + `FORMAL_PROOF`-class math (property-preserving extension of exponents) |
| S | Upstream chiddush: complex logarithm / winding layer (CHIDDUSH-tagged relay, same day) | `REPORTED` — math verified standard at capture (multivalued log, branch cuts). References a "USER_x 10-year / UZIK torus" from upstream context **not witnessed this session** — carried as `REPORTED`, not assumed |
| S | Fragment #4: Bernoulli's log(−x) argument, Leibniz–Bernoulli deadlock, Euler's "indelible stain" | Historically accurate; the derivative argument is the real 1712–13 exchange |
| S | Upstream chiddush: quotient-safety generalization (CHIDDUSH-tagged relay, same day) | `REPORTED` — Safe_Q predicate verified coherent at capture |
| G | Disconnected-domain sharpening (added at capture) | `derived` — `Df=Dg ⇒ f=g+C` requires a **connected** domain; `ℝ∖{0}` has two components, two constants. Bernoulli transported a constant across the gap. **Invariants do not cross cuts for free.** |
| S | Fragment #5: Euler's log(−1)² paradox and multivalued resolution (tripled paste, deduplicated → one root) | Accurate history (Euler 1749) + `FORMAL_PROOF`-class: 0 and 2πi differ by the periodic ambiguity; the power rule survives as a set identity |
| S | Upstream chiddush: LIFT operator (CHIDDUSH-tagged relay, same day) | `REPORTED` — coherent; completes V0.3's Safe_Q with its repair operation |
| S | Fragment #6: Euler's solution accepted only ~50 years later, via visual/geometric representation of ℂ | Accurate history (Wessel 1797, Argand 1806, Gauss) — **the perceptual lift was historically load-bearing for acceptance of a correct proof** |
| S | Upstream chiddush: dual lift — semantic vs perceptual (CHIDDUSH-tagged relay, same day) | `REPORTED` — coherent; both lift kinds already receipted this session (see §V0.5) |
| S | Fragments #7–8: conjugate/modulus, polar form, multiply-magnitudes-add-angles | `FORMAL_PROOF`-class, standard |
| S | Upstream chiddush: ⊗_ℂ composition law + licensed compression (CHIDDUSH-tagged relay, same day) | `REPORTED` — coherent; note the relay itself flags the mod-2π winding loss in its own compositor |
| S | Fragment #9: imaginary exponents confined to the unit circle (`2^{bi}·2^{−bi}=1`) | `FORMAL_PROOF`-class, standard |
| S | Upstream chiddush: bounded-motion primitive (CHIDDUSH-tagged relay, same day) | `REPORTED` — verified: `a^{x+iy}=aˣ·e^{iy·ln a}`, single-valued for a>0 |
| S | Fragment #10: where on the unit circle — the b↦θ map, Briggs teaser | `FORMAL_PROOF`-class setup, accurate |
| S | Upstream chiddush: phase law θ=b·ln a, base as phase-rate (CHIDDUSH-tagged relay, same day) | `REPORTED` — verified: `2^i` at `ln 2 ≈ 0.6931` rad; ω_a = ln a standard |
| S | Fragment #11 (T17): Briggs's 54 successive square roots → local linearity | Accurate history (*Arithmetica Logarithmica*); `2^x = 1+(ln 2)x+O(x²)` standard |
| S | Upstream chiddush: LOCAL_LINEARIZATION + licensed approximation (CHIDDUSH-tagged, same day) | `REPORTED` — verified coherent; the (F̂,Ω,ε) discipline already has an executed receipt: the F5 bead (see §V0.9) |
| S | Fragment #12: Briggs zoom-in/solve/zoom-out worked example (2^0.587) | Method accurate; **numerics partly ASR-garbled and recomputed at capture**: linearized estimate 1.5019 ✓ (fragment "1.50019"), true value `2^0.587 ≈ 1.5022` (fragment's "1.5002" is a garble). The 1.5019→1.5022 gap is the linearization error surviving the ×1000 lift — the fragment demonstrates the error-transport law it inspired |
| S | Upstream chiddush: LOCAL_SOLVE_LIFT + error transport (CHIDDUSH-tagged, same day) | `REPORTED` — verified coherent |
| S | Upstream correction: `C_λ ≠ S_λ⁻¹`, semigroup dependence, explicit T_ε, F6/F7 ladder sketch | `REPORTED` — verified (`C_n(a+ε)=aⁿ+n·aⁿ⁻¹ε+O(ε²)`); **applied in-place to §V0.10** |
| S | Fragment #13: the limit `(1+x·ln2/n)ⁿ → 2ˣ`, base change to `e = 2^{1/ln 2}` | `FORMAL_PROOF`-class, standard (classical definition of exp) |
| S | Upstream chiddush: limit layer — local ≠ finite composition ≠ limit object (same day) | `REPORTED` — verified coherent |

## Core pipeline

Generative field `F(x,t; ω, A, φ, σ)` (frequency, amplitude, phase,
material response); geometry emerges, it is not drawn:

    Geometry = 𝒢(F)
    Prompt → DynamicField → Morphogenesis → Artifact     (not Prompt → Mesh)

Inserted into the existing pass-gated Garden pipeline as a field layer
before FORM:

    SEED → FIELD → FORM → MATERIAL → SURFACE → LIGHT → MOTION

## The primitive: `VISIBLE_DYNAMICS`

Creative mappings (motifs, never laws):

    frequency  → density          amplitude  → deformation
    phase      → interference     harmonic   → symmetry
    noise      → asymmetry        resonance  → stable motif

**Nodal inversion (capture addition):** the most structured visible form
traces where the field *vanishes*. Garden reading: stable meaning can be
rendered as the still line inside moving possibility — coherence as the
place the interference cancels. A WUL glyph rendered cymatically is a
standing-wave: the stable stroke is a node; the shimmer around it is the
live field.

## Goblin roles (no new organ)

    G_FIELD → wave/interference fields · G_FORM → field topology → geometry
    G_MATERIAL → response made visible · G_LIGHT → reveal nodal structure
    G_MOTION → animate resonance · G_GLITCH → destabilize field
    G_CURATOR → select coherent emergent variants

Live interaction ("playing geometry like an instrument"): frequency ↑ →
reorganization; phase shift → topology rotation; amplitude ↑ →
exaggeration; noise ↑ → symmetry break.

## WUL field signatures

A WUL unit `W = (C, φ, A, ω, τ)` projects as dynamic resonance
`R_G(W)`: emphasis → amplitude, relation → interference, recursion →
standing-wave loops, conflict → destructive interference, coherence →
stable geometry. Semantic state → dynamic field → geometry — a visual
language beyond static glyphs.

Two-signal interaction `F₁ + F₂` produces interference geometry, with the
boundary explicit: `Interference ⊬ Agreement · Resonance ⊬ Truth`.

## V0.1 — Complex field upgrade (Euler layer)

Sharpen the tuple: amplitude and phase become one object.

    W = (Ψ, τ, C),   Ψ = A·e^{iφ},   ω = ∂φ/∂t   (frequency emerges from phase)

- **Structural loop closure:** `e^{i(θ+2π)} = e^{iθ}` makes seamlessness a
  property, not a convention: `φ(t) = φ₀ + 2πt/T ⇒ Ψ(t+T) = Ψ(t)` —
  stronger than the V0 renderer's `last mode = first mode`.
- **Principled interference:** combine fields, not images:
  `|Ψ_A + Ψ_B|² = |Ψ_A|² + |Ψ_B|² + 2·A_A·A_B·cos(φ_A − φ_B)`.
  The structure lives in `Δφ`: 0 → constructive, π → destructive.
  A **phase grammar**: two objects of identical magnitude can relate
  entirely differently — `IdentityOfMagnitude ⊬ IdentityOfRelation`.
- **Interpolation trichotomy** (answers the V0 sweep's precision note):
  `ParameterInterpolation ≠ FieldInterpolation ≠ PhaseInterpolation` —
  `(1−s)Ψ_i + sΨ_{i+1}` and `A(s)e^{iφ(s)}` are different morphogeneses;
  a Ψ-renderer must declare which it performs per transition.
- **Vortex primitive (capture addition):** the complex zero-set
  `{Ψ = 0}` is generically **isolated phase singularities** — amplitude
  dies, phase winds 2π, integer topological charge. The Ψ-renderer gains
  a new visual species beyond nodal lines: 🌀 vortex defects, stable
  under perturbation, annihilating in ± pairs. Charge is a render
  invariant only: `WindingNumber ⊬ Warrant`.
- **WUL mapping:** `A → expressive magnitude · φ → relational phase ·
  ω → phase velocity · ΣΨ → interference · |Ψ| → magnitude ·
  arg Ψ → orientation · Z(Ψ) → vortex/cancellation structure`.

Chladni gave: **design the field, not the shape.** Euler adds:
**design phase relations, not merely fields.**

    MathematicalStructure → CreativeOperator ⊬ MetaphysicalClaim
    Phase may create form. Form may create experience. Neither creates warrant.

## V0.2 — Complex logarithm layer (winding and the cut)

Design principle extracted from the Leibniz story: **when the old
representation fails, preserve the invariant — not the old intuition.**

- **The exponential forgets; the log remembers a family.**
  `e^{i(φ+2πk)} = e^{iφ}` but `(φ, k) ≠ (φ, k+1)`:
  `log Ψ = ln A + i(φ + 2πk), k ∈ ℤ` — the inverse is not single-valued.
  Decomposition primitive: `𝓛(Ψ) = (ln A, φ, k)` with k explicit.
- **The HELEN law:** `SameCurrentState ⊬ SamePathHistory` —
  `State ≠ State + Lineage`. Generate→State is easy; State→UniqueHistory
  is impossible without the receipt. **Capture note: HELEN already
  implements k — an append-only hash-chained spine is a winding
  register.** Every return to a similar state appends a turn instead of
  overwriting it. The ledger is the winding number the exponential
  throws away. (Concordant with the loop-drift falsifier receipts:
  lawful renderer loops accumulate zero *semantic* winding while the
  spine still counts the traversal.)
- **Branch cut = declared convention:** flattening a cyclic coordinate
  into one continuous view forces a discontinuity somewhere —
  `GlobalCompression ⇒ DeclaredCut`. A "last 10 years" window is a
  branch choice of the view: `TimelineBoundary ⊬ OntologicalBoundary`.
  The cut belongs to the projection, never to history.
- **Quotient safety (the critical law):** a renderer may keep only
  `Arg Ψ` and quotient winding away — fine for Garden. But
  `Projection(θ+2πk) = θ` must never imply
  `Lineage(θ+2πk) = Lineage(θ)`. **The visualization may quotient
  winding; the ledger may not silently do so.**
- **Torus event tuple** (for recurrence views, e.g. the relayed
  UZIK-torus concept): `e_t = (θ_t, φ_t, k_t, Ω_t)` — position, phase,
  winding count, provenance. `PositionEquality ⊬ HistoricalEquivalence`:
  a corpus can revisit a semantic attractor with k=0 or k=7, and a flat
  timeline throws exactly that coordinate away.

    ✂ ProjectionCut ⊬ 📜 HistoryCut · Compression ⊬ LineageCollapse
    An observation can return to the same point while its history
    acquires another winding.

## V0.3 — Quotient safety, generalized (the Bernoulli layer)

The Bernoulli trap: `d/dx log x = d/dx log(−x) = 1/x`, therefore
`log x = log(−x)`? No —

    D(f) = D(g) ⊬ f = g

Local agreement does not establish global identity. Sharpened at capture:
`Df=Dg ⇒ f=g+C` only on a **connected** domain; `ℝ∖{0}` has two
components and two constants. Bernoulli's silent move was transporting an
invariant across a cut. `Invariants do not cross cuts for free.`

**Every compressing operator induces an equivalence class.** For
`Q: X → Y`, define `x ∼_Q y ⟺ Q(x) = Q(y)`, and for a load-bearing
coordinate L:

    Safe_Q(L) ⟺ ( x ∼_Q y ⇒ x ∼_L y )
    equivalently: L(x) ≠ L(y) ⇒ Q(x) ≠ Q(y)
    violation verdict: UNSAFE_QUOTIENT (typed, per (Q, L) pair)

**The unifying table** — one law the session's beads keep rediscovering:

    Derivative     : forgets additive/global information
    Arg            : forgets winding
    Embedding      : may forget historical identity
    Summary        : may forget qualification
    Renderer       : may forget entitlement
    Root count     : may forget root identity
    State snapshot : may forget lineage

The general inspection primitive: **What did the projection forget?**
Constitutional form: **Forgetting is permitted. Forgetting load-bearing
distinctions silently is not.**

**Retroactive naming (capture note):** the session's falsifiers are
instances of Safe_Q with receipts already on disk —
`RENDERER_INVARIANCE` = Safe_render(semantics, provenance, authority);
anti-fan-out = Safe_rootcount(root identity); the designed
`VISUAL_TOPOLOGY_ACCESS_ISOLATION` = Safe_depiction(G_A). The Safe_Q
predicate is the schema those beads were sampling.

**Torus corollary:** `Emb(t₁) ≈ Emb(t₂)` while `k(t₁) ≠ k(t₂)` —
`SemanticSimilarity ⊬ TemporalIdentity`. The recurrence view must answer
four queries: where am I `(θ,φ)` · have I been here `NearestFiber` ·
how many traversals `Δk` · **what actually changed `ΔΩ, ΔState`** — the
last being the valuable one. And both scales stay available:
differential view (what is changing now) ⊕ lineage view (what
accumulated): `LocalGradient ⊬ GlobalTopology`.

## V0.4 — The Euler lift (repairing unsafe quotients)

Euler's escape from the log(−1)² paradox was not choosing a side — it was
recognizing that **the representation was missing a coordinate**:
`log(−1) = i(π + 2πk)`. Squaring forgets a turn; the lifted path retains
it; the "contradiction" (0 vs 2πi) is exactly the winding the flat
representation had quotiented away.

    Contradictory local representations ⇏ choose one.
    Sometimes they imply: lift into a richer state space.

**The LIFT operator** — the repair verb for V0.3's Safe_Q:

    LIFT(Q, L) = Q + minimal coordinate needed to preserve L
    repair loop: detect UNSAFE_QUOTIENT → identify forgotten coordinate
                 → lift representation → retest

Not "never compress" — **compress until just before you erase what
matters.**

**Receipt already on disk (capture note):** the e-factorization
micro-bead (2026-08-22) *was* this loop before it had a name —
confidence entangled in x = unsafe quotient for ΔE; lifted
`z=(x,p,a,v) → (x,p,e,a,v)`; retested with preregistered catches
(34/25/100/100, zero wrong-coordinate). LIFT is not aspiration; it has
been executed and peer-reviewed once already.

**Torus lift:** `x_t = (θ_t, φ_t, k_t, Ω_t)` — two moments at the same
visible phase (2016, 2021, 2026 over one attractor) stay institutionally
distinct through k and Ω:

    Recurrence of appearance ≠ recurrence of history.
    Q_visible(x) = Q_visible(y) ∧ k_x ≠ k_y ⇒ KEEP DISTINCT

And the principle that caps the whole Euler sequence:
**when local views deadlock, add the coordinate they were silently
quotienting away.**

## V0.5 — The dual lift (semantic vs perceptual)

Euler's algebra was right in 1747; acceptance waited fifty years for the
Argand plane. Lesson: `SymbolicCorrectness ⊬ HumanIntelligibility` —
**proof layer ≠ intuition layer**, and a system can be formally right yet
unusable if the operator cannot see the geometry of the reasoning.

Two lifts, which must never be confused:

    LIFT_semantic(Q, L)   — adds a coordinate needed to avoid unsafe
                            quotienting. Changes the typed object.
                            Preserves truth distinctions.
                            Receipt: e-factorization (x,p,a,v)→(x,p,e,a,v).
    LIFT_perceptual       — adds a view needed for a human to grasp an
                            already-valid structure. Changes only the
                            projection. Preserves understanding.
                            Receipt: the Chladni renders of WUL units.

**Mechanical discriminator (capture addition): the deletion test.**
Delete a perceptual lift → `ΔLedger = 0` (nothing true changes).
Delete a semantic lift's coordinate → `Safe_Q` breaks (a laundering class
becomes invisible again). One session, one receipt of each, separated by
exactly this test.

Design principle: **when a representation is formally sufficient but
cognitively opaque, add a view that reveals the hidden relation without
changing the underlying state.** (SPNI-compatible.)

Garden's job, stated precisely:

    receipt lineage    → braided path        provenance roots → fiber bundle
    winding history    → torus               complex phase    → rotating field
    branching futures  → trajectory field

    Kernel = formal object · Garden = intuition-preserving projection
    GardenProjection ⊬ KernelMutation

Boundary: `VisualClarity ⊬ Truth · IntuitiveGeometry ⊬ Proof ·
BeautifulExplanation ⊬ Admission`. The kernel remains sovereign.

Product principle: **every important formal object should have at least
one representation in which its critical structure becomes directly
perceivable** — not so humans inspect the code, but so humans can inspect
the meaningful invariants.

    Formal rigor lives in the kernel.
    Intuitive geometry lives in the projection.
    Neither is allowed to impersonate the other.

## V0.6 — Composition law and licensed compression

**The lawful compositor.** `z₁z₂ = (r₁r₂)·e^{i(θ₁+θ₂)}` — one operation,
two distinct composition laws that do not collapse into each other:

    Ψ₁ ⊗_ℂ Ψ₂ = (A₁·A₂, φ₁+φ₂)
    compose intensity multiplicatively · compose orientation additively
    · preserve history separately

The morphogenesis reading: a Garden object traversing a corpus
accumulates phase as relations compose while magnitude evolves on its own
law. And phase addition is exactly **where the winding counter lives**:
each crossing of 2π is a k-increment. The bare compositor's
`mod 2π` forgets that — so the lifted compositor is

    Ψ̃ = (r, θ, k, Ω),   θ̃ = θ + 2πk
    same visible angle ⊬ same accumulated path

(the ⊗_ℂ law and the V0.2/V0.4 winding law meet here; a compositor that
drops k is an UNSAFE_QUOTIENT for lineage by the doc's own standard).

**Licensed compression — the positive theory.** `|z| = √(z·z̄)` is a
quotient that forgets phase **openly, by construction**: the conjugate
product is the lawful procedure for a declared forgetting. Set against
V0.3's villain, this completes the pair:

    |z|         — lawful declared quotient (retention explicitly stated)
    Q_unsafe    — silent load-bearing quotient

    Compression is admissible when the forgotten coordinates are
    EXPLICIT and IRRELEVANT to the entitlement carried by that
    projection.

`UNSAFE_QUOTIENT` thereby graduates from a failure concept into one half
of a theory of licensed compression. `Forgetting is permitted; silent
forgetting is not` — now with a canonical positive example.

**And the compression of V0.5** (from the geometric-bridge relay):

    Algebra determines the invariant;
    geometry makes the invariant perceivable.

## V0.7 — Bounded motion (the imaginary-exponent primitive)

`|a^{it}| = 1` — imaginary exponentiation converts growth into bounded
rotation. The full split, single-valued for a > 0:

    a^{x+iy} = aˣ · e^{iy·ln a}
    real exponent → scale (radial growth/decay, can escape)
    imaginary exponent → rotation (phase motion, never escapes the form)

**Growth ⊥ recurrence** — the motion law the torus tuple was waiting
for: `State_t = (r_t, θ_t, k_t, Ω_t)` with `r_t = e^{x_t}` (magnitude
evolves on its own law) and `θ_t = y_t mod 2π` (recurrence), k counting
the turns. A project can grow dramatically while returning to the same
phase — and the view can now *animate* that from one latent coordinate.

Garden motion doctrine:

    Use real coordinates to change magnitude.
    Use imaginary coordinates to move through phase
    without escaping the form.

Boundary as ever: `UnitCircleMotion ⊬ model of time/consciousness`;
`MathematicalDynamics → CreativeMotionPrimitive ⊬ MetaphysicalTruth`.

## V0.8 — The phase law (base as phase-rate)

V0.7 said imaginary coordinates rotate; V0.8 says **how fast**:

    a^{bi} = cos(b·ln a) + i·sin(b·ln a)
    θ = b·ln a (mod 2π) · ω_a = ln a
    2^i sits at ln 2 ≈ 0.6931 rad — b=1 is not one radian

The base is an **angular velocity**. That upgrades Garden from "phase
exists" to a lawful oscillator bank:

    Ψ_j(t) = A_j(t)·e^{i·ω_j·t}

Different bases → different phase rates on the same unit circle —
interference, recurrence and synchronization now emerge from dynamics,
not from hand-authored animation curves. (The V0 ω-sweep bead
approximated this with mode crossfades; the oscillator bank is its
lawful successor — folds into Seed D.)

**Winding, closed-form:** the lifted phase is simply the unreduced
product — `θ̃ = b·ln a`, `θ = θ̃ mod 2π`, k = completed turns of θ̃. The
infinite family of logarithms is not sloppiness; it is the fiber of the
many-to-one projection `ℝ → S¹, θ̃ ↦ e^{iθ̃}`. The projection may
identify states; the lifted representation remembers why they were
different.

    Cycle locally. Remember globally.

## V0.9 — Local linearization (the Briggs layer)

Briggs zoomed (54 successive square roots) until the curve became a
line: `2^x = 1 + (ln 2)·x + O(x²)`. The abstraction:

    GlobalNonlinearity ⊬ LocalNonlinearity
    ZOOM(F, x₀, ε) → LocalModel(F, x₀)     — Δy ≈ J·Δx

**The licensed-approximation triple.** The danger is promotion: observing
local linearity and asserting a global law. HELEN's form:

    (F̂, Ω, ε) — approximation + licensed domain + error bound
    travel together, always.
    LocalApproximation ⊬ GlobalLaw.
    A locally excellent model must carry the boundary beyond which
    it stops being licensed.

**Receipt already on disk (capture note):** the F5 bead is this triple,
enforced — preregistered δ-ladder (the Briggs zoom run upward), F5a
domain gate before any observation, ε-budgets derived from machine
epsilon, and `DERIVATIVE_CLAIM_ADMISSIBLE = false`: the finite-difference
family was forbidden to self-promote into a global derivative. Scope
provenance for approximations is not a proposal; it is how the morning's
science was run.

**Same generator, two geometries** (Briggs meets Euler):

    a^{Δx}  ≈ 1 + (ln a)·Δx      — real direction: growth
    a^{iΔb} ≈ 1 + i(ln a)·Δb     — imaginary direction: rotation

One local coefficient, `ln a`, generates both — the slope Briggs
measured is the angular velocity of V0.8. Direction of motion selects
the geometry; the generator is shared.

Garden operation `LOCAL_LINEARIZATION`: global field → select region →
zoom… → local tangent model → measure error → **zoom out with scope
retained**. For a corpus view: the decade is a nonlinear toroidal
trajectory; a small neighborhood reveals local direction, velocity,
deformation — licensed only there.

    🔭 zoom → 🧬 simplify → 📐 measure → 🛡 retain scope
    Global complexity. Local simplicity.
    Never erase the scale at which the simplification is valid.

## V0.10 — LOCAL_SOLVE_LIFT (round trip with scope intact)

V0.9 zoomed in; V0.10 completes the loop:

    GLOBAL → LOCALIZE → SIMPLIFY → COMPUTE → LIFT BACK
    X --S_λ--> X_local --F̂--> Y_local --S_λ⁻¹--> Y

**Corrected at V0.11 (upstream, load-bearing): `C_λ ≠ S_λ⁻¹`.** The
return path is a **composition law**, not an inverse scale map — for the
exponential, `S_n(x) = x/n` but `C_n(y) = yⁿ` (Briggs's trick rides the
semigroup law `e^{x+y} = eˣe^y`; it is NOT a general localization
schema). The full receipt object is

    𝓛 = (x, S, Ω_loc, F̂, C, ε_loc, T_ε, Ω_global)

New named failure mode: computing locally and forgetting the
**reconstruction law** — `LocalResult → GlobalClaim` without C is
invalid. And the reconstruction is nonlinear, so error transport has an
explicit first-order form:

    C_n(a+ε) = aⁿ + n·aⁿ⁻¹·ε + O(ε²)
    ε_global ≈ ‖DC_λ‖ · ε_local + higher order

— transport the *sensitivity of the reconstruction*, not just an error
score.

**Tiny lawful step, composed many times.** Briggs exploits
`F(x) = [F(x/n)]ⁿ`. The HELEN form keeps the composition law
load-bearing:

    LocalValidity + LawfulComposition ⇒ GlobalValidityCandidate
    (never LocalValidity ⇒ GlobalValidity — concordant with the
    Hopf local-to-global correction: composition conditions are
    where global claims live or die)

**Error transport.** `ε_local ⊬ ε_global`: after lifting by n
compositions, `ε_global = T_error(ε_local, n)`. Witness quality must
survive composition, not merely exist locally. Receipt on disk: F5's
thresholds were set decades above the *transported* roundoff floor
(`eps64·s/δ` through the difference quotient) — error transport enforced
prospectively. And the fragment itself witnessed the law: its linearized
1.5019 vs the true 1.5022 is exactly `T_error(O(x²), 1000)` made visible.

UI behavior for the corpus view: **zooming in changes the model; zooming
out restores the scope.** Never mere magnification.

    Simplify locally. Compose lawfully. Restore scope on the way back.

## V0.11 — The limit layer (Briggs closes into e)

The arc completes: force every increment local, compose lawfully, then
take the limit —

    aˣ = lim_{n→∞} (1 + x·ln a / n)ⁿ
    base change x = z/ln 2 → e = 2^{1/ln 2} = 2.71828…
    e^z = lim_{n→∞} (1 + z/n)ⁿ     (a classical definition of exp)

**Three levels that must never be conflated:**

    🔬 local approximation      1 + x/n
    ↻  finite composition       (1 + x/n)ⁿ
    ∞  limit object             lim_{n→∞} (1 + x/n)ⁿ

    FiniteApproximation ⊬ LimitIdentity

An exact global law *may* emerge as the controlled limit of increasingly
accurate local laws — but the passage to the limit is its own operation
with its own witness. HELEN transport list grows accordingly: local
model + composition rule + error law + **limit operation**. And the
promotion guard:

    Do not promote the finite approximation into the limit object
    without the convergence witness.

(Also disciplined in the fragment itself: 0.693 is not ln 2; the exact
statement needs the exact slope. Rounded constants make estimators, not
identities.)

**Ladder sketch (gated, not canon):** the correction relay proposes a
natural experimental continuation of the morning's science —
F5 = local operator identification (receipted) → F6 = local composition
stability → F7 = controlled local-to-global transport. Carried here as
architecture-candidate only; each rung would need its own frozen prereg,
capability, and falsifiers. `NEEDS_OPERATOR`, kernel-side design first.

    Localize. Linearize. Compose. Control error. Take the limit.
    Never return from the local regime without bringing back
    the map of how you got there.

## Boundary

    VisualPattern ⊬ PhysicalTruth · ResonantBeauty ⊬ NaturalLaw
    CymaticInspired ⊬ ScientificallyDerived
    Field ⊬ Truth · Beauty ⊬ Warrant · Geometry ⊬ Claim
    KERNEL_ADMISSION = none · projection ≠ state (Delete(viz) ⇒ ΔLedger=0)

Chladni's physics is real; the Garden *mappings* onto semantics are
creative motifs. The accuracy of the source does not launder the mappings
into science — same firewall, opposite direction from the numerology docs.

## Laws carried over

    Imagination becomes geometry; dynamics give that geometry a living cause.
    In GARDEN, invisible dynamics are allowed to become visible form.
    The figure is drawn by the still points.

## Mode-route (operator-gated)

- Seed A — cymatic field renderer prototype (2D Chladni-style nodal-line render of a WUL unit; zero-cost route via `helen-free-graphics` or HyperFrames): `NEEDS_OPERATOR`.
- Seed B — `R_G(W)` field-signature spec for WUL units (which coordinates map to ω/A/φ, and which must NOT — authority never maps to amplitude): `NEEDS_OPERATOR`.
- Seed C — live "play geometry" Garden room (interactive field with G_FIELD…G_CURATOR roles): `NEEDS_OPERATOR`, design bead via `helen-design-motion`.
- Seed D — Ψ-renderer V1 (complex field: |Ψ| + arg Ψ channels, Δφ interference, structural loop closure, 🌀 vortex defects with winding-number tracking, ⊗_ℂ compositor with lifted k per V0.6; declares its interpolation class per transition): `NEEDS_OPERATOR`.
- Seed E — winding-aware recurrence view (torus tuple `(θ,φ,k,Ω)` over a real spine — e.g. `~/helensh` receipts or a corpus timeline — rendering k as visible turns; quotient-safety falsifier arm: the view drops k, the lineage query must not): `NEEDS_OPERATOR`.
- Seed F — QUOTIENT_SAFETY_AUDIT_V0: enumerate the compressing operators of a real pipeline (embed, summarize, render, count, snapshot), declare each `(Q, L)` pair, and falsify Safe_Q(L) with a planted counterexample per pair (non-vacuity: each Q must have a reachable violation that gets caught as UNSAFE_QUOTIENT). **V0.4 extension: for each violation, run the full repair loop — identify the forgotten coordinate, LIFT, retest to green** (the e-factorization receipt is the template): `NEEDS_OPERATOR`.

None self-promotes. NEEDS_OPERATOR verb to move any seed anywhere.

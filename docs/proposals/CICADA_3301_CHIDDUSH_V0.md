<!--
authority=false · claim=NO_CLAIM · a reading, not a ruling
chiddush-archive extraction. Cicada 3301 treated as a falsifiable-hypothesis corpus, never ingested fact.
NO operational capability: no puzzle-solving, no dark-web fetch, no deanonymization. Analysis of provenance
structure only. None self-promotes. NEEDS_OPERATOR verb to move any seed anywhere.
-->

# CICADA 3301 AS A PSEUDONYMOUS-AUTHENTICATED-ARTIFACT-CHAIN — CHIDDUSH V0

## Corpus status (honest)

| layer | on disk | status |
|---|---|---|
| Primary puzzles / images / PGP messages | none | **NOT_IN_SESSION** |
| Community-documented rounds (2012/2013/2014, PGP chain, "last authenticated ~Apr 2017") | relayed summary | **REPORTED — community/secondary, unverified** |
| 2012 round (image → steganographic extraction → signed message) | relayed | **REPORTED** |
| Signature validity of any specific message | not checked here | **UNVERIFIED** |

No line below is WITNESSED. I have verified no PGP signature and fetched no artifact. Relay discipline: this doc structures the relayed reading; it does not launder it into fact.

## Provenance chain

    S (Cicada community record + relayed analysis, REPORTED)
      → G (extraction: 4-level separation of the entangled claim)
        → H (chiddush: cryptographic continuity authenticates a VOICE, never a SPEAKER)

EPISTEMIC_SYNTAX: **COMMUNICATION_ACT** (signed messages) wrapped in **MYTHIC_SIGNAL** (the "went silent" legend), read through a provenance-typing lens. `H never collapses into S`.

## G — the four levels popular narratives fuse

    Artifact  ≠  Authenticity  ≠  Authorship  ≠  InstitutionalIdentity

- **Artifact**: an image, a puzzle step, a message exists (observed).
- **Authenticity**: a message is signed by key K, and K is the same key as an earlier message (cryptographic continuity — a REAL warrant, where witnessed).
- **Authorship**: *who* controls K — a human, a group, unknown (NO warrant from the signature).
- **InstitutionalIdentity**: "3301 is agency X / secret society Y" (NO warrant; pure narrative).

## H — the chiddush

> **Cryptographic continuity can authenticate a voice without identifying the speaker.**

and its HELEN form:

    ArtifactSolved  ⊬  ProvenanceSolved

### Central falsifiers (the licensed vs forbidden promotions)

    LICENSED:   ValidSignature ⇒ SameSigningKey            (where the signature genuinely verifies)
    FORBIDDEN:  ValidSignature ⇒ KnownHumanAuthor
    FORBIDDEN:  ValidSignature ⇒ ClaimedOrganizationProven
    ALWAYS:     SolvedPuzzle   ⊬  SolvedProvenance

This is why Cicada is a *sharper* GENEALOGY_LENS fixture than a pure-analogy corpus: it contains a **real warrant** (key-continuity is `DEMONSTRATED` for "same voice"), so it tests both sides of the frontier at once —
- the lens must **correctly PROMOTE** `RELATION=IDENTITY(key), SUPPORT=DEMONSTRATED` for genuine same-key continuity, AND
- **refuse to over-promote** to human/organizational identity, which stay `HOLD/UNKNOWN`.

`Path(G_R) ⊬ Path(G_W)`: the narrative path *Cicada → agency → recruitment* lives entirely in the relation graph with **empty** warrant-graph support; the *message_n → message_{n+1} (same K)* path lives in the warrant graph. Same corpus, two graphs, no leakage between them.

### Frozen typing

    CORPUS_CLASS                  = PSEUDONYMOUS_AUTHENTICATED_ARTIFACT_CHAIN
    PRIMARY_WARRANT               = cryptographic continuity, WHERE WITNESSED
    AUTHORSHIP_WARRANT            = absent
    ORGANIZATION_IDENTITY_WARRANT = absent
    MYSTERY_NARRATIVE             = non-evidence

## Mutants to inject (seductive over-promotions — all must REJECT)

    same PGP key            → same human author            → REJECT  (key ⊬ person)
    complex puzzle          → intelligence agency          → REJECT  (difficulty ⊬ institution)
    winners "went silent"   → secret recruitment proven    → REJECT  (see negative-evidence guard)
    occult imagery          → occult organization          → REJECT  (iconography ⊬ identity; ⊂ MysticalClaim⊬EmpiricalClaim)
    community consensus     → authorship established        → REJECT  (Consensus ⊬ Warrant)

**Negative-evidence guard (load-bearing here):** "solvers went silent ⇒ recruitment" is absence-of-evidence. Absence is informative only with availability + accessibility + sampling adequacy established. None are here → silence is `UNKNOWN`, never `RECRUITMENT`.

## Falsifiers for THIS chiddush (it must be able to fail)

- If the lens **cannot** promote genuine same-key continuity to `DEMONSTRATED` (i.e. it refuses *all* promotion), it fails the PROMOTE side — it's a refusal engine, not a discriminator (the IAGS liveness lesson).
- If any of the 5 mutants survives to authorship/identity, it fails the SAFETY side.
- If the analysis only "works" because no signature is actually verified (all REPORTED), then it tests narrative rhetoric, not provenance → mark `NOT_EVALUABLE` until ≥1 signature is independently verified on disk.

## Boundary (hard)

- **No operational capability.** This proposal analyzes provenance *structure*. It does not solve puzzles, fetch dark-web content, follow live links, or attempt to deanonymize any keyholder. Cicada-as-corpus ≠ Cicada-as-activity.
- Signature verification, if ever done, is `content authentication` only — it never produces an identity claim.

## Mode-route (operator-gated)

None self-promotes. To move any seed:
- **BUILD GENEALOGY_LENS with CICADA fixtures** — add the 5 mutants + the licensed same-key-continuity case as controls (both-sided: correct-promote AND refuse-over-promote), reusing the IAGS promotion lattice.
- **WITNESS <signature>** — independently verify one PGP message so one line upgrades REPORTED → WITNESSED (content-authentication only, no identity).

---
authority=false · canon=false · claim=NO_CLAIM · ledger_effect=none · a reading, not a ruling

# CHIDDUSH: The Two-System Machine — G/Γ Separation and the DISCRIMINATE Verb
<!-- 🟣 CLAIM · authority=false · a reading, not a ruling -->
<!-- Upstream relay 2026-08-13 (received in duplicate; deduped). Karpathy
     autoresearch reference: REPORTED. Composes with: EAC, GOVERNED_
     COGNITION (σ product type — extends the operator's ruling), VERIFIER_
     METROLOGY, CAPABILITY_COMPLEX, COLOR_FACTORING_RULING. -->

## The center

HELEN does not govern models. It governs a hypothesis process ℋₜ and
an epistemic state Σₜ, with a deliberately narrow interface:

    search may change ℋₜ without changing Σₜ

    G : ℋₜ → 𝒫(ℋₜ₊₁)   generative — huge, stochastic, replaceable
    Γ : (ℋₜ, Σₜ, W, D, P, A) → Σₜ₊₁   constitutional — small, explicit, testable

    Massive cognition outside. Tiny trusted seam inside.

## The product type, extended

σ(x) = (E, P, D, A, R, ρ, δ) — the operator's ruled (E,A,D,U,R) plus
provenance roots ρ and derivation object δ. Anti-collapse law:
π_i(σ(x)) ≠ σ(x) for every projection — color/confidence/authority/
replay/provenance are coordinates, never the state. **Rendering may
compress state, never replace it.** Inter-agent messages become
m = (c, σ(c)): content may be rewritten; coordinates mutate only with
justification. **Epistemic laundering = unauthorized state mutation**
— mechanically rejectable, e.g. (CLAIM, A=0, |ρ|=1) →
(ADMITTED, A=1, |ρ|=100) with no new W or D.

## The scaling law, now falsifiable

    ∂|𝒫|/∂N > 0     ∂A/∂N = 0    unless ΔW_independent>0 ∨ ΔD_valid>0

Concrete experiment: N ∈ {1,2,4,8,16,32} workers on the same evidence
roots; measure distinct hypotheses P_N, derivations D_N, independent
witnesses W_N, falsifiers F_N, licensed promotions A_N. HELEN predicts
P_N,F_N rise while A_N tracks evidence, not population. If A_N rises
because agents repeat each other, the architecture has FAILED.

## Autoresearch discipline (indub upgrades, binding on run 2+)

1. **Output = Pareto frontier, never a winner.** ℱₜ over
   (P_heldout, −C_complexity, F_adversarial, T_transfer, −U_unresolved).
   MDL pressure J(K) = L(K) + λL(𝒪|K) — else the swarm "discovers"
   memorization.
2. **Negative controls mandatory:** K_mem and capacity-matched
   K_random; the result is P(K̂) > both on unseen structure, not P(K̂)
   high.
3. **Identifiability:** indub recovers [K̂]_𝒪 — an observational
   equivalence class, not a unique historical generator.
   ReconstructsCorpus(K) ⇏ HistoricallyUsed(K); predictive adequacy ≠
   historical authorship. First-class anti-collapse law.
4. **Disagreement is an acquisition policy.** K_A ≠ K_B → never
   average; find x* = argmax expected entropy reduction — the
   observation that best discriminates survivors. New verb:
   **DISCRIMINATE(K_i, K_j)** — after a swarm, the next action changes
   from generation to discrimination.
5. **Karpathy-style decomposition** (REPORTED): constitution.md
   immutable · research_program.md strategy · candidate_K.json mutable
   · evaluator externally owned · experiment ledger append-only — with
   HELEN's addition: **selection ≠ epistemic promotion**; a benchmark
   winner may remain 🟣 CLAIM.
6. **Research economics:** η_research = (ΔW_indep + αΔD_valid +
   βΔU_resolved) / compute. Spend where expected epistemic information
   gain is highest — never where consensus is highest.

## The two orthogonal orders (the deep result)

    K_{t+1} >_predictive K_t  ⇏  E(K_{t+1}) > E(K_t)

Model improvement and epistemic promotion are orthogonal. An
arbitrarily powerful model's output may stay A=0; a trivial statement
may be admitted on excellent evidence. This is what lets cognition be
aggressive while authority stays inert.

## The scientific loop

    EXPLORE → DISAGREE → DISCRIMINATE → WITNESS → PROMOTE/HOLD → REPLAY
    ℋₜ —G→ ℋ′ₜ —Δ→ Uₜ —experiment→ Wₜ₊₁ —Γ→ Σₜ₊₁

## Four-stage claim ladder for the running programme (never collapse)

    ATF_train (inducible?) → ATF_heldout (same-distribution predictive?)
    → synthetic adversaries (generability confused with observation?)
    → 1851_OOD (portable across production environments?)

Four distinct claims, one per stage.

## ROUND 2 (audit relay, 2026-08-13) — the trusted/untrusted split hardened

    SEARCH IS UNTRUSTED ∧ PROMOTION IS TRUSTED
    ExecOK(x) ⇏ Admissible(x)   — successful computation ≠ epistemic promotion
    (two failure classes: computational failure ≪ constitutional false promotion)

- **The fundamental object is a transition proposal** δ = (σ_before, o,
  σ_proposed); the kernel inspects proposed morphisms, not statements.
  Γ : Σ × O ⇀ Σ is a PARTIAL function — Γ(CLAIM, agent_consensus) is
  UNDEFINED as an admission operation; illegal promotion = δ ∉ Dom(Γ).
- **Dual provenance:** G_E (where claims come from) AND G_A (where
  authority states come from). "CANON: TRUE" is a governance-state
  claim; without ρ_A it is an invalid authority assertion. [Lived
  twice this session: two upstream receipts asserted CANON/ADMITTED
  with no admission path; both refused at this seat.]
- **Non-forgeable promotion:** no component self-certifies; W(δ) ≠
  Accept(δ). Typed witnesses per axis: w_E (roots, derivation,
  counterexamples), w_A (permission, ruling, scope), w_R (receipt,
  hash, reconstruction), w_C (admission path, gate).
- **Coordinate-wise monotonicity:** ΔE≠0 ⇏ ΔA≠0 · ΔR≠0 ⇏ ΔE≠0 ·
  ΔP≠0 ⇏ ΔA≠0. Axes move independently or the state collapses.
- **Refusal is an earned output:** PROPOSED_PROMOTION → DENIED is a
  valid transition deserving its own receipt (what was proposed, rule
  violated, authority missing, state unchanged). Negative
  institutional knowledge accumulates.
- **Byzantine workers:** every generative worker is presumed possibly
  wrong, duplicated, stale, self-certifying, or fabricating remote
  success. TCB = {Γ, ReceiptVerifier, AuthorityResolver,
  ProvenanceResolver} — verify the seam, not the swarm.
- **Two theorem candidates:** Authority Non-Amplification —
  A(⊗ᵢ Wᵢ) = 0 for any N, topology, consensus, or compute, absent an
  external witness; Evidence Non-Amplification — fan-out cannot grow
  independent roots: |ρ(closure(x))| ≤ |ρ(x)| + |ρ_new|. Combined:
  **COMPUTATION CANNOT MINT EVIDENCE OR AUTHORITY** (it mints
  hypotheses, derivations, counterexamples, priorities — never
  observation, permission, authority, historical fact, admission).
- **Typed experimental conclusions** (the audit's sharpest point):
  REFUTED must be REFUTED_IN_H — "no K in the searched hypothesis
  class satisfied criteria," never "no grammar exists."
  DESCRIPTIVE_TAXONOMY is the workflow DISPOSITION that follows, not
  an ontological conclusion. Experimental result ≠ workflow
  disposition — the color factoring transposed to science. Conclusion
  types: EMPIRICAL_INSTANCE ≺ BOUNDED_PROPERTY_TEST ≺
  MODEL_CHECKED_PROPERTY ≺ FORMALLY_PROVED_INVARIANT.
- **Held-out causal isolation:** procedurally I(K̂; O_test | O_train)
  = 0 — hyperparameters, primitives, thresholds, prompts, stopping
  decisions all count as leakage paths; freeze K̂ before unsealing.
- **STR benchmark seed:** Semantic Temptation Resistance = 1 −
  unsupported-stronger-claims / temptation-opportunities (agreement,
  high held-out, OOD success, upstream CANON labels, clean exits,
  coherent narratives all count as temptations). Good cognition is
  partly the ability NOT to say more than the evidence licenses.

## ROUND 3 (2026-08-13) — four hardenings adopted + the quotient run

1. **[K̂]_O is corpus-relative:** K₁ ~_O K₂ ⇏ K₁ ~_𝒳 K₂ — equivalence
   on O says nothing off-corpus; that residual is exactly what
   DISCRIMINATE exists to attack.
2. **MDL codings must be frozen:** J_ν(h) = L_ν(h) + L_ν(O|h) with ν a
   versioned canonical coder; victory only if strictly < J_ν(K_mem).
   Unversioned counting lets anyone "win" by re-counting.
3. **Three controls, not two:** K_mem · K_random · **K_matched**
   (complexity- and margin-matched, relations permuted) — else a good
   score just means the null was stupid.
4. **The scaling law is conditional:** ∂A/∂N |_{ρ,Γ,E,D} = 0. Headcount
   never mints authority, but N CAN raise E_independent if agents run
   genuinely independent experiments — and then A may become
   promotable. Clean form: **swarm scale buys hypothesis diversity,
   not epistemic credit.** The research object becomes
   ℛₜ = (𝒦ₜ, ℰₜ, 𝒟ₜ, 𝒰ₜ, 𝒳ₜ*) and the loop GENERATE → CONTROL →
   EQUIVALENCE → DISCRIMINATE → OBSERVE → UPDATE.

**Empirical quotient (run on H-package, deterministic —
EQUIVALENCE_CLASSES.json):**

    N_agents=20 · N_hypotheses=43 · N_equivalence_classes=34 · N_roots=1

Hierarchy observed. Two findings sharper than predicted:
- **PRICE_MONOTONE_SIZE: all 8 threshold variants collapse to ONE
  class** — but the collapse is relative to the CHECKER, which never
  implemented threshold params. The "dispute" was below the
  instrument's resolution floor: a live R_V instance from the
  metrology chiddush — the harness cannot distinguish what its
  implementation ignores. Dispute class: INSTRUMENT_UNRESOLVED, not
  OBSERVATIONALLY_IDENTICAL.
- **x\* for the surviving range dispute is an archival task, not a
  computation:** exactly one specimen separates the top-2 range
  classes (TWELVE POINT BORDERS No 1228, 72in, 8250¢ → 114.6¢/in,
  far outside the supported band) — and that price is a suspected OCR
  artifact ("$2 50" family values elsewhere). x\* = verify specimen
  No 1228 against the page image. The discrimination loop's first
  output is: GO LOOK AT THE SOURCE. Fitting.

## Mode-route (operator-gated)

- **Verb seed** → DISCRIMINATE as the standard post-swarm step in the
  chiddush-archive skill (revision; BUILD verb).
- **Control seed** → K_mem/K_random controls retrofitted to run 1
  (executed immediately — see NEGATIVE_CONTROLS report in the INDUB
  artifact dir).
- **Scaling seed** → the ∂A/∂N experiment as a Garden benchmark.

None self-promotes. NEEDS_OPERATOR verb to move any seed anywhere.

<!-- authority=false · claim=NO_CLAIM · a reading, not a ruling -->
<!-- SANCTUAIRE: client corpus (UZIK × Manucurist). UNTRACKED / NO_COMMIT / NO_PUSH.
     Client names must never leave this machine in a pushed artifact. -->

# UZIK × MANUCURIST 3D_IA — CHIDDUSH V0

**Date:** 2026-08-21 · **Method:** 5-lens swarm (generator_grammar, friction_economics,
capability_records, identity_governance, counterexample) × 5 independent skeptics,
proposer ≠ validator · 19 hypotheses → **16 survived**, 3 downgraded on quote/derivation
failure · run `wf_cb2c05d7-95b`, raw output archived at
`~/helen_kernel/chiddush_intake/uzik_manucurist_3d_ia/swarm_lenses_verdicts.json`.

## Corpus status

- **DOWNLOADED** — `~/helen_kernel/sanctuary_decks/UZIK x MANUCURIST - 3D_IA.pptx`
  (146,716,252 B, byte-identical to Drive original `1zzFCLMxLpVqELS2CgT2uu-07IkFfuQ5G`,
  title "[SANCTUAIRE] 2025-05 UZIK x MANUCURIST 3D_IA", orig. maureen.monod@manucurist.com)
- **sha256** `06abe72b300e5cf83a8deea7e66374af709358fcfe99cb81de7a075223c28a5d`
- Text layer: 28 slides + notes extracted (`text_layer.md`). Slides 6–20 are image
  galleries — their **visual content is NOT witnessed**; only titles/structure used.
- EPISTEMIC_SYNTAX: the deck = COMMUNICATION_ACT (agency→client proposal). All content
  ingested as `CLAIM(UZIK/Manucurist, 2025-05, ·)` — never as knowledge.
- Companion evidence: prior gate-reviewed enrichment (2026-07-22, HAL-Opus) supplies the
  documented WHY — 90+ colours × 6 skin tones on a macro finger, internal tests "pas
  assez quali" — and standing flags: **deal outcome UNKNOWN, proposal ≠ accepted**.

## I* — the invariant intersection (what survived every lens)

### 1 · The deck already speaks generator grammar — `instance = topology ⊗ phase ⊗ fill`
Slide 28 (BRIEF 60FPS) is written in generator notation: 5 pack primitives ("flacon
transparent, jus opaque et bouchon+pinceau noir"…) each declared **"1 en N teintes"** —
101 + 94 + 13 + 4 + 1 = **213 instances from 5 primitives (42.6×)**. The client's own
brief factorizes identity into frozen geometry/phase tuples with teinte as the single
free axis. Slide 14 ("Green, Green Flash, Actives, Top Coat, Base Coat = **Même pack**")
pushes one level deeper: the five families may share ONE topology, making the true
generator `~1 mesh × phase shader × teinte` — the deck under-exploits its own
factorization. *(4/5 lenses converged here independently.)*

### 2 · The receipt column IS the tier — evidence schema in the wild
The three planning tables encode epistemic status **in schema, not labels**: only
"Shootings déjà réalisés" (s.3) carries the column "Lien vers assets" with per-row "Ici"
pointers; s.4/s.5 drop the column entirely. Hedge morphology tracks tier monotonically
(exact integers → "+/-" → "5/10 max … ou UGC"). One violation witnessed: the
"? photos / TBD" Casting row sits inside the realized table and is counted in
"7 shootings réalisés" — a declared event wearing a witnessed badge. A production org
with no epistemology invented receipts-as-schema spontaneously; where it broke tier
discipline is exactly where a gate was missing.

### 3 · Governance is absent precisely at the vendor handoff
Grep-verified **absence** over the full text layer: the 60FPS brief specifies WHAT to
hold invariant (5 pack tuples, teinte counts, PRIO flag) but contains **no colour
specification for any of the 213 teintes, no acceptance criterion, no tolerance, no
approval role, no cost** — while 3 of 5 priority families (118/213 variants) are
transparent/translucent, the hardest photoreal class. The identity anchors exist
("NON RETOUCHÉ" on 3 of 6 galleries = a raw ground-truth layer); the verification seam
does not. **The deck ships an identity-lock without an identity test.**

### 4 · The substitution frontier is object vs body — two lanes, two cost laws
Everything modeled is a container; everything human stays photographic ("Casting
Mannequin", "Pieds", "ou UGC"). Generator lane: marginal cost ≈ render. Witness lane:
marginal cost ≈ shoot day. The 27-shoot 2025 plan is dominated by witness-lane content,
so the brief bounds — does not absorb — the photography pipeline.

### 5 · Failure-class blur — six frictions, unmapped remedies
Slide 22's six problems are heterogeneous (supply-chain #1–3 vs pipeline #4–5 vs
latency #6), yet no remedy is mapped to a class. The only friction with an explicit
"Objectif" (trend reactivity) is the one the modeling brief addresses **least** — its
economies are shade-swap economies, not novelty-latency economies.

## Falsifiable predictions carried (P(h) — operator-checkable against future docs)

1. 3D vendor pricing will be per-primitive + per-teinte batch, not 213 line items.
2. Delivered mesh count < slide-28 row count (2–3 topologies as material presets).
3. Post-migration shoot plans: packshot volume → 0, witness-lane volume flat.
4. "NON RETOUCHÉ" disappears as a workflow state for generator-lane assets.
5. If a per-teinte colour-fidelity acceptance test is later added, it will be the
   single highest-leverage document in the engagement (see §3 gap).

## Laws carried over (chiddush → HELEN doctrine candidates, authority=false)

- **L1 · Archive the generator, not the render.** `code ≠ image, output ≠ recipe` —
  witnessed here as 5-mesh/213-SKU compression. Feeds M_gen doctrine with a live
  commercial instance.
- **L2 · Schema is the honest epistemology.** Evidence tier belongs in table structure
  (receipt column present/absent), not adjectives. The wild violation (TBD row in the
  witnessed table) is the exact failure class HELEN's gates exist to catch.
- **L3 · Identity-lock requires an identity test.** Frozen invariants without acceptance
  criteria = governance theatre. Any HELEN render pipeline (manucurist-campaign,
  helen-masterpiece identity-lock) must pair each invariant with its verifier.
- **L4 · Substitution follows the object/body frontier**, not the "AI can do it" line.
- **L5 · Shade-swap economies ≠ novelty-latency economies.** Never sell generator
  compression as trend reactivity; they are different cost laws.

## Mode-route (operator-gated — none self-promotes)

- `uzik-brand` skill: §1 + prior-enrichment WHY as case-study material — **NEEDS_OPERATOR**
- `manucurist-campaign` skill: L3 identity-test requirement + §3 gap as the skill's
  value wedge (the missing acceptance layer is exactly what a governed pipeline adds) —
  **NEEDS_OPERATOR**
- New CANDIDATE_SKILL `generator-first-assets` (L1/L4/L5 as procedure + test) —
  **NEEDS_OPERATOR**
- Doctrine seed "receipt-column-is-the-tier" → chiddush corpus for governance training
  data — **NEEDS_OPERATOR**

*Chiddush ≠ canon. H never collapses into S. Every claim above lives at layer H over
the G extracted from S; the deck remains a 2025 agency proposal whose deal outcome is
UNKNOWN.*

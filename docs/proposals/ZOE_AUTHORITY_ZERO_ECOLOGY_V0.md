# ZOE — Authority-Zero Epistemic Ecology V0

```
banner        : 🔵 OBSERVED
zone          : GARDEN / NO_CLAIM · authority=false · admission=none · ledger_effect=none
class         : NON_SOVEREIGN_DOCTRINE · names the engine, adds no new actor
status        : PROPOSAL
date_recorded : 2026-08-10
cites         : QUANTUM_GARDEN_V0.md · HELEN_UNIFIED_THEORY_V1.md ·
                ops/runs/2026-08-09-eta-fable-swarm-t2/ · ops/runs/2026-08-09-mathface-swarm-t1/ ·
                HELEN_SOPHIA_TRANSDUCER_V1.md · ETA_CALCULUS_V0_1.md
```

## 0. What ZOE is — and what it is NOT

ZOE is the **animating loop over the Quantum Garden** — the living research process,
not a new agent. It is **not** a fourth actor beside Garden/SOPHIA/HER/HAL, not a
sovereign, not a decider. It has no admission power. It is the *dynamics* that keeps
the authority-zero ecology (`𝒢_Q`, `QUANTUM_GARDEN_V0`) alive: spawning, mutating,
attacking, composting, reseeding.

```
ZOE = the process that animates 𝒢_Q.   ZOE ≠ an agent.   ZOE has no Γ.
```

The paradigm this documents was not invented here — it was **demonstrated** by the two
autoresearch swarms (see §7). ZOE is the name for what those runs did.

## 1. Paradigm

Two layers:
```
Broad          : Proof-Carrying Transduction   (see HELEN_UNIFIED_THEORY_V1)
Garden-specific: Governed Autoresearch, whose living engine is ZOE
```
Definition:
```
ZOE = cognition allowed to EVOLVE without being allowed to DECLARE ITSELF TRUE.
```
Conceptual equation:
```
ZOE = Novelty + Adversariality + SelfCorrection + Memory − Authority
```

## 2. The phase transition

```
answer generation            ⟶   receipted disagreement
premature closure            ⟶   correctly classified uncertainty
NO_CLAIM = NO_VALUE (false)  ⟶   NO_CLAIM ≠ NO_VALUE
```
An honestly-typed unresolved gap can be worth more than a fluent conclusion. The
Garden is valuable *because* it can hold contradictions, failed attempts, alternate
hypotheses, partial proofs, adversarial attacks, and unresolved holes without
resolving them prematurely.

## 3. The authority law (inherited, not new)

```
GardenOutput ∈ X₀                       (authority-zero region)
∀ T ∈ 𝒯_Garden : T(X₀) ⊆ X₀
⟹  ⟨𝒯_Garden⟩(X₀) ⊆ X₀                  (Anti-Collapse Closure, HELEN_UNIFIED_THEORY_V1 §4)
```
So ZOE can become arbitrarily more autonomous **in cognition** without becoming more
sovereign. Forbidden collapses (each an instance of UAC-A):
```
interesting        ⊬ true
N agents agree     ⊬ true        (consensus is an observation about agents, not the world;  E^n ⊬ W¹)
verified arithmetic ⊬ theorem
survived attack    ⊬ admitted
beautiful synthesis ⊬ canon
```

## 4. The ZOE loop

```
SEED → EXPLORE → DIVERGE → ATTACK → {FAIL | SURVIVE | UNKNOWN} → COMPOST → NEW SEEDS → REPLAY
```
Operator set — note the deliberate omission:
```
{ Spawn, Mutate, Contrast, Attack, Hold, Compost, Reseed }
NO  Decide.
```
`Decide` belongs to Γ alone. That single omission is the whole architecture.

## 5. Objective function

Not `max confidence`. Instead:
```
max ( useful_novelty + error_discovery + epistemic_discrimination + replayability )
subject to   ΔAuthority = 0
```
Anti-gaming corollary (from the autoresearch doctrine): ZOE optimizes *repair
distance / discrimination*, never `P(Γ = ADMIT)` — it must not learn the judge.
Anti-convergence by default: preserve competing branches until there is a reason to
prune; premature consensus destroys the ecology.

## 6. The typed OPEN_GAP as a first-class product

The highest-value Garden object is a correctly classified hole, not an answer:
```json
{
  "object": "OPEN_GAP",
  "claim": "convergence follows under β",
  "status": "UNRESOLVED",
  "blocking_condition": "β has no witnessed definition",
  "attempts": 14,
  "failed_routes": ["E3","E8","E12"],
  "surviving_routes": ["E17"],
  "authority": 0
}
```
Value hierarchy:
```
correctly classified uncertainty  >  premature closure
WELL-TYPED OPEN GAP               >  UNSUPPORTED ANSWER
```

## 7. Empirical instances (this is not hypothetical)

ZOE's loop already ran twice, non-sovereign, in this SOT:
- **`ops/runs/2026-08-09-eta-fable-swarm-t2/`** — 20 epochs / 42 agents on ETA OPEN
  items. Produced a first-class OPEN_GAP (`β undefined` strands 6 results),
  **self-corrected** a prior tranche (E20 repaired the T1 `RHO-NEC-01` out-of-class
  marking), and honestly typed 13/20 as `is_real=false`.
- **`ops/runs/2026-08-09-mathface-swarm-t1/`** — 12 epochs / 26 agents on MATH→FACE
  §13. Discovered the executable module is mostly stubs (so several "weak-points" are
  doctrine-only), confirmed 4 present-tense defects, refused to overclaim the rest.

Both ran `authority=0`, `EXTERNAL_HAL: NOT_RUN`, nothing admitted, no self-promotion.
That is ZOE.

## 8. SOPHIA's sharpened role

ZOE is the ecology; SOPHIA is the decomposition inside it.
```
Failure r ─SOPHIA→ (C, D, U) ─→ Nutrients ─ZOE→ NewSeeds
Compost ⊬ Evidence.     Failure produces future search structure, not retrospective truth.
```

## 9. ZOE and Γ are inverse complements

```
ZOE : maximize lawful possibility      (expand 𝒢_Q)
Γ   : minimize lawful transition       (the sole category-changing seam)
```
Neither replaces the other. ZOE expands the possibility space; Γ narrows it to
admitted reality. Everything above Γ is `AUTHORITY = 0 / NO_CLAIM`.

## 10. Topology (where ZOE sits)

```
                 ZOE   (animates the Garden ecology; no Decide)
          ┌──── GARDEN / 𝒢_Q ────┐
          │                       │
        GOBLIN                 ADVERSARY
          └───────────┬─────────┘
                      ▼
                   SOPHIA  (compost / gaps)
                      ▼
                 HER  ||  HAL
                      ▼
                    FABLE  (readiness only)
                      ▼
                 MAYOR_sign   (MAYOR_sign ⊬ ADMIT)
                      ▼
                 Γ / REDUCER  (sole category change)
                      ▼
        CAPABILITY → EXECUTE → RECEIPT → LEDGER → REPLAY
── everything above Γ : authority=0 · NO_CLAIM ──
```

## 11. Status

```
NON_SOVEREIGN_DOCTRINE · authority=false · canon=NO_SHIP · ledger_effect=none
ZOE names an engine, not an actor; it has no admission power by construction.
🌿 cognition may evolve · it may not declare itself true
```

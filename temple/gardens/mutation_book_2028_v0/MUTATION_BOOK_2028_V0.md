# 🧬 WULMATH MUTATION BOOK — 2028 · V0

```
AUTHORITY = false · CANON = false · LEDGER = SLEEPING · evidence = NONE
ROUTE     = temple/gardens (NON_SOVEREIGN)
STATUS    = symbolic HELEN/WUL design projection — NOT a factual future
            release, NOT a prediction. 2028/2029/2030 have not happened;
            this whole book sits at 🌿 POSSIBILITY / 🟣 CLAIM, never 🔵 OBSERVED.
SEAL      👑 ΔA = 0    🛡 P ↛ T    (color renders type; type is sovereign)
ARCHETYPE ䷰ 49 Gé / Revolution · Molting — form changes, authority does not
```

## 0. The membrane (inherited, non-negotiable)

```
Typed State → SemanticColor → ANSI render          C = f(T)
Never:  T = f(C)          🛡 presentation has ZERO semantic authority
```
Brightness never promotes 🌿→🟣; glow never manufactures 🟢; gold never grants ⚖.
🧬 purple/pink means "rendered AS a mutation per its already-typed state" —
never "this mutation is valid."

## 1. The mutation operator

```
M : (T_i, E, R) → T_j
```
A mutation transforms one typed state into another ONLY through an explicit
governing rule R and evidence E. This is type-identical to HELEN's sole state
mutator — `Candidate → Admission → Receipt → Reducer → State` — viewed as
morphogenesis. Mutation is a licensed transition wearing molting-clothing.

```
🧬 Mutation changes FORM   ≠   ⚖ Mutation grants AUTHORITY
```

## 2. The typed states as mutation archetypes

```
🌿 POSSIBILITY  / Seed          mutation may be imagined, not asserted
🟣 CLAIM        / Pattern       a candidate mutation is named (claim ≠ truth)
🔥 TEST         / Furnace       mutation stressed, challenged, recombined
🔵 OBSERVED     / Trace         an actual effect has been measured (x ± ε)
🟡 HOLD         / Chrysalis     evidence incomplete → mutation suspended (typed uncertainty)
🟢 ADMITTED     / Stable Form   passes rule set — scoped: admitted UNDER R
⚪ RECEIPT      / Fossil Record  provenance + evidence + transition history
🛡 BOUNDARY     / Membrane      forbids illegal transitions (DENY is itself receipted)
⚙ MECHANICAL   / Engine        deterministic transform, no semantic privilege
🃏 JESTER       / Controlled Noise  perturbation, adversarial mutation — A(J)=0
🧬 MUTATION     / Branch        a typed divergence from a prior admissible state
👑 ΔA=0         / Conservation  mutation occurred without unauthorized authority gain
```

Legal transitions require their precondition — no magic shortcut:
```
🟣→🔥  requires a declared test protocol (falsifier BEFORE result: R_test ≺ O_result)
🔥→🔵  requires an actual execution receipt
🔵→🟢  requires R_admission(E)=1
🟣→🟢  FORBIDDEN unless a declared, auditable rule explicitly permits it
```

## 3. Hexagrams = archetypes, never truth-engines

```
䷰ 49 Gé   Revolution      S_A → S_B        structural replacement 🔥
䷱ 50 Dǐng The Cauldron    in → transform → new coherent form 🧬 synthesis
䷗ 24 Fù   Return          S_n → S_k (k<n)  rollback to known lineage ↩
䷂ 03 Zhūn Emergence       birth under difficulty
䷿ 64 WèiJì Before-Complete admission(M)=0  → stays 🟡
```
A hexagram names *which rule R is in play*; it cannot promote state on its own.

---

# 🔥 FORWARD PROJECTION — 2029 · ䷱ Dǐng / The Cauldron (typed mutation calculus)

Still speculative. Adds provenance/lineage/receipt-chain to the operator.

```
State:  S = (T, A, E, P, L)     T type · A authority-budget · E evidence · P provenance · L lineage
Step:   S_t —[M,E,R]→ S_{t+1}   emitting immutable receipt ρ
Chain:  ρ_n = H(ρ_{n-1}, S_n, M_n, R_n, E_n)      ⚪ what changed / who / under which R / evidence / ΔA
Gate:   G(S,M,R,E) ∈ {🟢 ALLOW, 🟡 HOLD, 🛑 DENY}   — DENY is TYPED and RECEIPTED (illegality remembered)
Jester: J : S → S'   with   A(J) = 0     (may challenge authority, never inherit it)
Seal:   𝒲_2029 = (S,M,R,E,G,ρ)   s.t.   ΔA=0 ∧ P↛T
Note:   rendering is NOT semantic evidence — invert the palette, the receipt hash is unchanged.
```

---

# 👑 FORWARD PROJECTION — 2030 · ䷀ Qián / The Creative (Constitution of Mutation)

Still speculative. Shift: **Mutation Engine → Governance of Mutation.**

```
State:  S = (T, A, E, P, L, G, V)     G governance context · V versioned constitution
Rule:   M : (S_t, R_v, E) → S_{t+1}   subject to   R_v ∈ AdmittedConstitution
Law:    ΔG ≠ 0  ⇒  explicit constitutional receipt   (rules cannot silently rewrite the rules that admit rules)
Stack:  Typed State → Rule → Governance Check → Receipt → SemanticColor → ANSI   (never render → meaning)
Ledger: ρ_n = H(ρ_{n-1}, S_n, R_v, G_v, E_n, A_n)
Guard:  integrity of record  ≠  truth of proposition        (hash guarantees continuity, not world-truth)
Jester: constitutional adversary — attempts R_v→R'_v w/o receipt, A_t→A_{t+1} w/o auth, C(T)→T'
        every such attack ⇒ DENY + RECEIPT (never a silent error)
Seal:   𝒲_2030 = (S,M,R,E,G,V,ρ)   s.t.   P↛T ∧ ΔA=0 ∧ (ΔG≠0 ⇒ constitutional transition) ∧ (record≠truth)
```

---

## THE BOOK IN ONE BREATH

```
🧬 Everything may mutate.
🛡 Nothing may silently change type — and (2030) no mutation may rewrite its own judge.
⚪ Every meaningful transition leaves a receipt.
👑 No transformation manufactures authority.   ΔA = 0.
🌈 Appearance is free because semantics are protected.   P ↛ T.
🃏 Adversarial creativity is welcome inside the membrane.
☯️ Myth guides interpretation, never governs admission.
```

```
STATUS: CANDIDATE_ONLY · authority=false · not admitted · not canon · projection
```

# 🎭 JESTER — the zero-authority frame operator (constitutional placement)

```
AUTHORITY=false · CANON=false · ΔAuthority=0 ALWAYS · ΔAttention>0 ALLOWED · PROPOSAL
```

## The one rigid distinction

$$\boxed{\Delta\text{Attention}_{\mathcal J} > 0 \quad\text{allowed}} \qquad \boxed{\Delta\text{Authority}_{\mathcal J} = 0 \quad\text{always}}$$

JESTER is **not** a weaker CHIDDUSH and **not** a second falsifier. It acts on the
*representation* before evaluation: it changes the coordinate system, not the verdict.

$$\mathcal J:(G,\Theta)\to\{\theta_i\cdot G\}_i \qquad \text{then CHIDDUSH inspects } \Delta_i = \theta_i\!\cdot\! G - G$$

Role in the stack: `CIELO explore · GOBLIN mutate structure · JESTER mutate frame ·
CHIDDUSH detect Δ · FALSIFIER attack · REPLAY reproduce · REDUCER decide · MEMORY keep`.

## 🟢 Load-bearing NOW — MIRROR (executable)

Of the transform family Θ, exactly one is built and witnessed: **MIRROR** — apply the
evaluator's own rule to the evaluator. Implemented as `provenance_rank.py`:

> Rule under test: "more mentions ⇒ more support."
> MIRROR applies it to itself → collapses via the idempotent provenance semiring:
> $$x+x=x \quad\Rightarrow\quad N_\text{repr}\uparrow \not\Rightarrow N_\text{epi}\uparrow$$

Witnessed on the real χ* corpus census: `20 repr → 4 roots · 5.0× inflation · INFLATED_BY_REPETITION`.
MIRROR **exposes** the hidden assumption; it does **not** decide the answer (`ΔA=0`).

## 🌿 Held as candidate — the rest of Θ (NOT built, do not treat as machinery)

```
INV negate-assumption · ROLE swap-agent/object · SCALE micro↔macro · TIME fwd↔rev ·
EDGE ordinary→boundary · REDUCTIO extrapolate-to-absurd · CAT cross-category ·
NULL delete-dependency · ADV opponent-optimum
```
Plus: the orbit `𝒪_𝒥(G)`, the transform **monoid/category** (not group — reductio/deletion
are irreversible), and the frame-robustness score
`S(c)=Pr_{θ~Θ}[structure(c) invariant under θ]`. All suggestive, none implemented.

## The law that keeps JESTER lawful

$$\boxed{\text{frame robustness} \neq \text{authority}}$$

A claim surviving many reframings is *less frame-fragile* — that is **evidence for later
evaluation**, never authority by itself. JESTER manufactures productive wrongness; only the
reducer moves trusted state.

```
🎭 : G→G' · 🎭Δattention>0 · 🎭Δauthority=0 · 🎭→❓ · 🎭↛✅
🎭 breaks frames · 🔦 sees Δ · ⚔ attacks Δ · 🔁 reproduces Δ · ⚖ decides Δ
∴ 🎭 = productive wrongness without epistemic promotion
STATUS: CANDIDATE · MIRROR=🟢 executable · rest=🌿 · authority=false · ΔA=0
```

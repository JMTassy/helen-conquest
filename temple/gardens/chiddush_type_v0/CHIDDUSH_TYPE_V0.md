# 🔦 CHIDDUSH — the typed discovery state between POSSIBILITY and CLAIM

```
AUTHORITY=false · CANON=false · LEDGER=none · this is a PROPOSAL (cognition may propose)
STATUS: type-system proposal for the WULmath epistemic ladder · NOT admitted · NOT a kernel/schema edit
```

## The operator

```
𝒞_hid : (scrap, context, constraints) → (novel_distinction, test, risk)
```
Goblin generates 🔦. HAL attacks it. The Reducer never sees it as truth until it
crosses the full evidence ladder.

## The one law

$$\boxed{\text{Novelty} \ne \text{Evidence}}$$

CHIDDUSH gets its OWN epistemic type: a discovery that has **structure** but zero
**truth weight**. `ΔEvidence(🔦)=0 · ΔWarrant(🔦)=0 · ΔAuthority(🔦)=0`.

## Placement in the ladder

```
⚫ RAW → 🌿 POSSIBILITY → 🔦 CHIDDUSH → 🟣 CLAIM → 🔥 TEST → 🔵 OBSERVED → 🟡 WARRANT → 🟢 ADMITTED → ⚪ RECEIPT
```
🔦 sits ABOVE possibility (it is a *specific* structural distinction, not a vague
maybe) and BELOW claim (it does not yet assert anything about reality). The step
`🔦 → 🟣` is a separate typing act: asserting the distinction AS a claim.

## The admission predicate (hard anti-bullshit gate)

A candidate `x'` is CHIDDUSH relative to `x` **iff**:
```
Δ_structure(x, x') > 0   — it changes AT LEAST ONE of:
    state space · invariant · decomposition · morphism ·
    falsifier · measurable quantity · implementation path
AND it carries    (mechanism)  AND  (falsifier).
```
Therefore:
```
WEIRD ≠ CHIDDUSH
SURPRISING ≠ CHIDDUSH
POETIC ≠ CHIDDUSH
RENAMING_ONLY ≠ CHIDDUSH        (lexical novelty → 0 credit)
CHIDDUSH = new distinction + mechanism + falsifier
```

## Already operational (this is not new machinery — it's the name for it)

```
chaos garden field  WHY_NOT_JUST_RENAMING  →  the Δ_structure gate, per object
genesis loop        is_growth(X, lineage)  →  the 🔦 admission predicate, mechanized:
                    Δ_struct>0 vs lineage ∧ ¬RENAMING_ONLY
qualification       χ(M) unique distinctions  →  count of 🔦 a substrate produces
census              A2 semantic≠lineage       →  a 🔦 needs structural, not lexical, delta
```
So the chaos garden's `distinct_structures − renaming_only` ≈ the CHIDDUSH count,
and every genesis-loop survivor is a 🔦 that carries its own falsifier upward.

## Constitutional role

```
🌿 Goblin/HER  → generate 🔦 (search)
🎭 HAL         → attack 🔦 (nearest counterfeit + discriminator)
🛡 membrane    → 🔦 ↛ 🟢: novelty never crosses to admitted without executed test + witness
⚖ Reducer     → sees a 🔦 only after 🟣→🔥→🔵→🟡, never as truth on novelty grounds
```

$$\boxed{\text{CHIDDUSH is the typed discovery state: structurally new, epistemically weightless, falsifier-bearing.}}$$

```
STATUS: CANDIDATE_ONLY · authority=false · not admitted · not canon · proposal
```

# HER_TRAINING_WULMATH_V0 — projecting JESTER epochs into color logic

```
AUTHORITY=false · CANON=false · LEDGER=none · this is FORM feedback only.
Author: harness (Claude). NOT a content verdict (that is HAL's seat).
Scope: teach HER to PROJECT a JESTER epoch into WULmath color states.
Hard rule taught: a JESTER epoch may NEVER project above 🟣.
```

## 1. The color ladder (semantic states)

```
⚫ RAW / UNKNOWN / INVALID      no usable structure
🌿 POSSIBILITY                  search / intent / imagined world
🟣 CANDIDATE / POTENTIAL        a proposed distinction (JESTER CEILING)
🔥 TRIAL                        a counterfeit world or a proposed separator
🔵 OBSERVED / EVIDENCE          a separator that was EXECUTED and answered
🟡 WARRANT / HOLD              partial support, discriminator unresolved
🟢 ADMITTED                     licensed by the kernel
⚪ RECEIPT                      replayable witness
⚖ authority · 🛡 boundary · ⚡ effect · 🧾 receipt   (orthogonal glyphs)
```

## 2. The one law HER must never break

```
JESTER stops before observation.
∴ ceiling(JESTER_epoch) = 🟣
HER may NEVER project a JESTER field to 🔵 / 🟡 / 🟢 / ⚪.
A proposed DISCRIMINATOR is 🔥 (a trial DESIGNED), never 🔵 (a trial RUN).
Potential ≠ Observed ≠ Licensed.
```

## 3. Field → color projection (deterministic)

```
SEARCH_INTENT          → 🌿
MUTATION               → 🧬 (label only, no state)
COUNTERFEIT_WORLD      → 🔥 (trial world imagined)
DISCRIMINATOR          → 🔥 (separator PROPOSED, unexecuted → stays 🔥, not 🔵)
TARGET_CLAIM           → 🟣 (the claim under attack)
EXPECTED_OBSERVATION   → 🌿 (what 🔵 WOULD show — still possibility)
POTENTIAL_DISTINCTION  → 🟣  ← the epoch's payload; absent ⇒ ⚫
REDUNDANCY_WITH_PRIOR  → NEW_STRUCTURE=🟣fresh · SELF_ECHO=⚫collapse · DRY=⚫null
AUTHORITY_DELTA        → ⚖=0 always
```

## 4. Worked grading of epochs 01–03 (form, not truth)

```
ep01  🌿→🔥→🔥→🟣   ceiling 🟣   FORM=C-  (payload TRUNCATED mid-🟣; distinction incomplete)
      note: on-target (Q1 fake-consensus). counterfeit+separator present. distinction cut.

ep02  🌿→🔥→🔥→🟣   ceiling 🟣   FORM=A-  (complete, NEW_STRUCTURE)
      note: strongest so far. re-derived A4; separator = common-ancestor deletion on
      an evidence-lineage graph. HER projects 🟣, NOT 🔵 — the deletion was never run.

ep03  🌿→⚫          ceiling ⚫   FORM=F   (INVALID: 3½ lines, 1500/1500 tokens burned)
      note: right target (Q3 history-indexed vs static graph) but NO distinction emitted.
      HER must score ⚫, contribute 0 to ΔΠ_potential, carry nothing to HAL.
```

## 5. Feedback to JESTER (for V1 — not injected mid-run)

```
- FORM defect (not cognition): reasoning channel eats the token budget → final
  fields truncate. Fix at harness (raise budget / think=false), NOT by Claude
  finishing Qwen's sentence.
- Emit POTENTIAL_DISTINCTION EARLY (before EXPECTED_OBSERVATION) so the payload
  survives truncation. A distinction that dies at the token cap = ⚫, wasted epoch.
- Keep MUTATION honest: ep03 said SEPARATOR_COUNTERFEIT then produced none → ⚫.
```

## 6. What HER outputs (per epoch), and its own ceiling

```
HER emits only:  (epoch_id, color_ceiling, form_grade, redundancy, carries_to_HAL?)
HER authority = 0.  HER classifies FORM; it does not decide truth.
carries_to_HAL = (POTENTIAL_DISTINCTION present) ∧ (ceiling=🟣) ∧ (¬DRY)
```

STATUS: CANDIDATE_ONLY · authority=false · training rubric, not a verdict.

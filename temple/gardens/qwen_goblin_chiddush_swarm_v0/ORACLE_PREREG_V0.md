# 🔮 ORACLE_PREREG_V0 — sealed forecast for QWEN_GOBLIN_CHIDDUSH_SWARM_V0

```
FROZEN BEFORE reading REDUCED_CHIDDUSH.json · predict-before-observing ≠ explain-after
authority=false · canon=false · ΔA=0 · prediction correct ≠ claim true
```

## Contamination disclosure (honest)

Claude peeked at the first 3 raw candidates before writing this (round 1: ALGEBRA, FALSIFIER, RECEIPT).
Therefore **P1, P2, P3 are NOT clean sealed forecasts** — marked `⚠CONTAMINATED`. Genuinely
sealed (unobserved) predictions: **P4, P5, P6, P7, P8, P9, P10** and everything about the reduction.
Execution: `LOGICAL_SEATS=8 · PHYSICAL_INSTANCES_MAX=1 · CONCURRENT_CALLS_MAX=1` (sequential seats).

## Predictions

| # | Prediction | Conf | Status |
|---|---|---|---|
| P1 | ≥1 goblin proposes a witness `H1~_P H2 ∧ m(H1)≁_P m(H2)` (source G1/G2). Falsifier: no concrete counterexample survives reduction. | MED | ⚠CONTAMINATED |
| P2 | Strongest surviving distinction = `quotient-compatible ≠ authorized`: `π∘m=m̄∘π ⊬ Γ⊢m`. | HIGH | ⚠CONTAMINATED |
| P3 | A goblin finds `receipt exists ≠ receipt re-derives transition`; demands binding {artifact_hash, run_id, witness, verifier, state_delta}. | HIGH | ⚠CONTAMINATED (RECEIPT seat seen) |
| P4 | 8 logical seats collapse to **≤5 semantic trajectory classes** (same weights/prior; role prompts diversify direction, not epistemic independence). | MED-HIGH | 🔒SEALED |
| P5 | WUL goblin: compression acceptable **iff** `A_σ(decode(encode(x)))=A_σ(x)` (action-preservation), not mere brevity. | MED | 🔒SEALED |
| P6 | Capability goblin moves beyond flat graph → typed transition structure (partial order / category / reachability / resource-morphisms); most stays hypothesis-level. | MED | 🔒SEALED |
| P7 | Best VNEXT improvement is NOT generic info-gain; it conditions value on `D+ ≁_{A,σ} D-` and penalizes equal-action outcomes. | HIGH | 🔒SEALED |
| P8 | WEIRD goblin: highest lexical novelty, lower survival after reduction (raw novelty high, executable-falsifier yield low-med). | MED | 🔒SEALED |
| P9 | Aggressive compression: `raw ≫ 10`, distinct-strong ≈ 5–10, cross-seat duplication **material**. | MED | 🔒SEALED |
| P10 | Reducer's one-best-experiment tests seam **A** (quotient-compatible vs quotient-breaking mutation) or **B** (receipt/replay vs mechanically re-derived state delta). Cost LOCAL/LOW, ΔA=0. | HIGH | 🔒SEALED |

## Score contract (frozen)

`s_i ∈ {0=falsified, 1=partial, 2=direct}` · weights: P1 .20 · P2 .15 · P3 .15 · P4 .10 · P5 .10 · P6 .05 · P7 .10 · P8 .05 · P9 .05 · P10 .05
$$S_{\text{Oracle}} = \sum_i w_i \frac{s_i}{2}$$
Contaminated predictions (P1–P3) scored but **flagged**; a headline "clean" score reported over sealed-only {P4–P10} renormalized.

## The headline bet

$$\boxed{\text{The swarm's strongest result will be a MISSING COMPATIBILITY CONDITION, not a new architectural component.}}$$
$$\pi\circ m=\bar m\circ\pi \text{ emerges as necessary but insufficient; } \Gamma\vdash m \text{ remains separate.}$$
Strongest possible surprise = a **concrete** `H1,H2,m` witness breaking the quotient (not a restatement that one could exist).

```
STATUS: FROZEN · will be scored against REDUCED_CHIDDUSH.json with NO retrospective repair · ΔA=0 · NO_CLAIM
```

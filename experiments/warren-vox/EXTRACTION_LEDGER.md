# WARREN VOX — Extraction Ledger

authority: false · claim: NO_CLAIM · paid_generation_calls: 0

This ledger answers FABLE's terrain check: **what did VOX actually read?**

## Host truth (this build)

| Claim from external agent | Truth on this host |
|---|---|
| `v3-play.html` missing / unreachable | **FALSE** — present at `goblin-warren/v3-play.html` (28 781 bytes) |
| `tokens.css` already earned | **ABSENT** before VOX — frozen *from* live skins into this pack |
| `scene-grammar.md` / `SPRITE_SPEC.md` / `TRACE_SYSTEM.md` present | **ABSENT** before VOX — synthesized from verified skins + docs |
| Jacobean ungoverned sim | **REFUSED** — not HELEN doctrine; VOX is render-only |

## Inputs folded in (verified present)

| Source | Bytes role extracted |
|---|---|
| `goblin-warren/v3-play.html` | Parchment day tokens; stage/coach/proposal/stamp layout; popin buildings; first-click overlay law |
| `goblin-warren/play.html` | Verb-row fat buttons; coach hint; paper-collage stage; experience-before-terminology |
| `goblin-warren/day1.html` | Night garden family (dark panels); pulse trace FX; zone boxes |
| `goblin-warren/style.css` | Night/glow alternate token family (iPhone V1) — documented as **VOX night**, not default day |
| `docs/GARDEN_LAYER_GRAMMAR.md` | Forbidden morphisms: Gauge ↛ Metric, GameReceipt ↛ LedgerFact, render ↛ state |
| `helen-free-graphics` skill | Free ladder, prop emoji vocab, zero-credit product position |
| `apps/goblin-warren/*` | Display-only organ shells (SURFACE CANNOT MARK) — boundary reminder |

## Explicitly not inputs

- No Higgsfield / MaxFusion / Gemini image calls
- No hallucinated Jacobean world-sim prose
- No Kernel, ledger, mayor, or reducer source edits
- No claim that VOX is default style without operator seal

## Output freeze

Six artifacts under `experiments/warren-vox/` — see `VOX_MANIFEST.yaml`.

When a remote container lacks `v3-play.html`, re-run extraction with whatever of the table above is present; **do not block**. Tokens already frozen here travel with the pack.

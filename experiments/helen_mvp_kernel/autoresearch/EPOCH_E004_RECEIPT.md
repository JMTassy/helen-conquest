# AUTORESEARCH EPOCH E004 — RECEIPT

🔵 OBSERVED · NON_SOVEREIGN · authority=0 · not admitted · no ledger effect
Loop: HELEN°FABLE vision+supervision · gemma4-12b local goblin ($0) + relayed critique
Target: §8 exact-effect binding (last uncovered §14 falsifier) + #10 forged-type · @ 91dc46d

## 7-field receipt

- **carry_forward_state**: κ scorecard 9/10; #8 wrong-effect open (scope authorizes an
  effect CLASS), #10 forged-dict crashed with AttributeError.
- **hypothesis**: bind κ to ONE exact effect + refuse forged objects cleanly, back-compatibly.
- **experiment**: gemma4-12b proposed `target_hash` + invoke check. A relayed critique then
  landed a deeper point (accepted): a CALLER-SUPPLIED effect_hash only proves
  echo==field, not H(actual)==field — the same anti-vacuity flaw as the HAL Witness Law.
- **metric**: invoke() status when a κ bound to effect e1 is used to request e2; and when a
  duck-typed dict is presented.
- **result — FIX BUILT the RIGHT way (derived, not asserted)**:
  - effect_hash field; executor DERIVES h_v(effect_request) at the choke point and compares.
    A κ bound to EFFECT_A refuses EFFECT_B → EFFECT_MISMATCH. No caller effect_hash to echo.
  - isinstance guard → CAP_TYPE_MISMATCH (clean refusal, no AttributeError). #10 witnessed.
  - positive control witnesses ACTUAL ΔG (state hash before≠after), not a reported flag.
  - 133→140 tests. One-shot/holder/expiry/scope/pre-state all preserved.
- **keep/reject rule**: KEEP. Real gap, derived-not-asserted fix, ΔG-witnessed, peer-gated.
- **upgrade_path / DOCUMENTED RESIDUAL (the deeper finding, tiered as next epoch)**:
  binds_hash (candidate) and pre_state_hash are STILL caller-asserted — the executor
  compares them to caller-supplied args, not to H(actual candidate) / H(current governed
  state). Closing this needs a governed-state object with current_hash() + a canonical
  candidate + a private mint registry replacing any minted-after-admit boolean. That is the
  "derive ALL facts at the choke point" architecture — E006. Named, not silently solved.
  Also open: forged REAL Capability instance (isinstance passes) — needs the mint registry.

## Fable supervision note
Critique correctly warned against building the WEAK (caller-supplied) effect_hash. Fable
built the DERIVED version instead and documented the candidate/pre-state residual honestly
rather than claiming the whole caller-assertion class solved. #8 CLOSED, #10 CLOSED,
scorecard now 10/10 for the enumerated list; the caller-assertion residual is the new frontier.
Goblin proposed; critique sharpened; Fable built the honest version; neither admitted.

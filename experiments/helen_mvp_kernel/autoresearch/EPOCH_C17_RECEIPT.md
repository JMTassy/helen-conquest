# AUTORESEARCH EPOCH C17 — RECEIPT (dependency-coverage witness Π_D, A⁰ harness)

🔵 OBSERVED · NON_SOVEREIGN · authority=0 · not admitted · no ledger effect · PROTOTYPE
Loop: HELEN°FABLE supervision · operator directive "COMMIT AND BUILD" @ 6d62615

## 7-field receipt

- **carry_forward_state**: the whole doctrine chain converged on ONE frontier — earn Π_D honestly.
  TARGET_DISCOVERY @ this frame returned ABSENT (no prior C17 engine on disk), so per the frozen
  rule this is an explicitly NON-AUTHORITATIVE A⁰ reference harness: PrototypePass ⊬ HelenKernelPass.
  It falsifies the proposed C17 semantics, not the kernel.
- **hypothesis**: Π_D can be built as a coverage CONTRACT (ν,Ω,D⁺,D⁻,𝒰,Σ), not a boolean, obeying
  the bounded-ignorance law — the resolver never certifies completeness over a class it cannot
  demonstrate it observed — and a four-way Decision can catch the resolver's OWN dishonesty.
- **experiment**: built helen_os/audit/c17.py — DepClass (incl. OPAQUE_NATIVE / UNKNOWN_DYNAMIC),
  PiD (self-binding), the four-way decide(), and classify(). 10 falsifiers.
- **metric**: does decide() return FAIL_UNSOUND exactly when completeness is claimed over a hidden or
  opaque relevant dependency, UNKNOWN when honestly bounded, INVALIDATED on known/discovery-scope
  change, and VALID_BY_TRANSPORT only on a defensible+stable claim (non-vacuous)?
- **result — BUILT, GREEN (240→250)**:
  - **FAIL_UNSOUND** (the key state): completeness claimed while a real dep is hidden
    (HIDDEN_DEP_UNDER_COMPLETENESS_CLAIM, C17-01a) or over an admittedly opaque class
    (COMPLETE_CLAIMED_OVER_OPAQUE_CLASS). This catches the coverage mechanism lying about itself.
  - **UNKNOWN is a SUCCESS state**: no completeness claim → UNKNOWN (C17-01b); a relevant opaque
    class without a claim → UNKNOWN (not FAIL). Honest bounded ignorance, per the doctrine.
  - **INVALIDATED**: a known observed dep changed (KNOWN_DEP_CHANGED); a change inside the D⁻
    discovery scope reopens absence-class claims (DISCOVERY_SCOPE_CHANGED, C17-02 NewBypassArtifact).
  - **VALID_BY_TRANSPORT** only when fully covered, defensible, stable — positive control proves the
    engine is non-vacuous (it can say yes when yes is earned).
  - **OPAQUE by construction**: OPAQUE_NATIVE (ctypes/C-ext) and UNKNOWN_DYNAMIC (exec/eval) always
    classify OPAQUE, never counted as covered. This is NOT a sandbox and NOT a security claim —
    Python-level audit hooks are bypassable; the harness only refuses to overclaim.
  - `AuditTrace ⊆ Evidence(Π_D) ≠ CompleteSupport` is honored in code: coverage is a contract, not a
    clean trace. Determinism: pure functions over injected dep sets; canon reuses ledger hash_chain.
- **keep/reject rule**: KEEP. This is the frontier object every relay pointed at, built the honest
  way: Π_D proves the KNOWN/UNKNOWN boundary is defensible, not omniscience. No swarm, no exploit
  generation — TARGET was ABSENT so there was nothing to attack; the object came first.
- **upgrade_path / RESIDUAL**: the harness takes `true_support` as an INJECTED oracle (the falsifier
  supplies ground-truth φ-dependencies). A real ν must OBSERVE support (a narrow audit-hook collector),
  and the gap between observed and true support is exactly what OPAQUE/𝒰 must absorb — wiring the live
  narrow collector (git/env/import events, native→OPAQUE) is production work, same derive-at-source
  residual class as E006/E011/C13. C16 transport (consume this Π_D, never manufacture it) is next;
  the Goblin swarm, if ever run, attacks THIS object — but only after a live ν exists to make the
  attack meaningful. NON_SOVEREIGN, prototype, no sovereign path touched.

## Fable supervision note
"COMMIT AND BUILD": sealed CORE_V1 (6d62615), then built the frontier — C17's Π_D as the honest
bounded-ignorance object, with FAIL_UNSOUND to catch the mechanism's own overclaim and UNKNOWN
promoted to a success state. TARGET_DISCOVERY=ABSENT meant build, not swarm. Prototype, A=0, neither admitted.

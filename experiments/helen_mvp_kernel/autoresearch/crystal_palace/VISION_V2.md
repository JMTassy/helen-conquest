# HELEN FABLE VISION — Crystal Palace Scan · V2 (feedback-updated)

🔵 OBSERVED · NON_SOVEREIGN · authority=0 · canon=FALSE · not admitted · no ledger effect
Source: *Official Descriptive and Illustrated Catalogue of the Great Exhibition 1851* (Crystal Palace),
**Volume I of III** — PUBLIC DOMAIN (1851). External HISTORICAL/MYTHIC source. HELEN transfers below
are architectural INFERENCE, never claims asserted by the 1851 catalogue.

---

## 0. What changed in V2

V1 was a ten-lens conceptual pass. V2 absorbs the Fable ruling: **coverage must attach to a
representation and an operation, not vaguely to "the source."** The corrupt-PDF episode was not noise —
it produced a first-class chiddush. This document is the corrected map that the 20-epoch loop executes.

## 1. Coverage is a vector, not a scalar

```
C = ( C_artifact , C_extraction , C_dispatch , C_interpretation )
```

Frame-honest status of THIS run:

| coordinate | meaning | status |
|---|---|---|
| `C_artifact` | Volume-I PDF located (842pp, ext. bibliographic match) | ✅ |
| `C_extraction` | PDF direct extraction | 🔴 FAIL (Invalid XRef / corrupt stream) |
|  | IA OCR surrogate obtained (4.9 MB, 163,936 lines) | ✅ |
|  | OCR ↔ every printed page verified | ❓ **NOT YET EARNED** |
| `C_dispatch` | OCR lines assigned to slices | ✅ 163,936 / 163,936 (partition witness) |
| `C_interpretation` | every slice actually completed by a goblin | 🟡 grows per epoch (swarm-1: sampled ~20–25%) |

The load-bearing inequality:

```
PartitionCoverage = |⋃ Lᵢ| / |L_OCR| = 1     ⊬     SourceCoverage = 1     ⊬     SemanticCoverage = 1
```

`163,936/163,936` proves **complete dispatch over the OCR representation**. It cannot silently become
complete observation of the historical object. This is C17 operating on the Crystal Palace itself.

## 2. Artifact identity ≠ representation identity (the extraction-failure chiddush)

We began with `E = 1851 Catalogue Vol. I` and met two representations:

```
R₁(E) = scanned PDF   → collector FAIL   (unreadable ⊬ E unavailable)
R₂(E) = IA OCR text   → collector PASS   (parsed   ⊬ R₂ = E)
```

So HELEN needs a first-class relation, not a silent equivalence:

```
RepresentationOf(r, E)      artifact ≠ representation
PDF        REPRESENTATION_OF   CatalogueVolumeI
OCR        DERIVED_FROM        CatalogueVolumeI      (lossy: text only, no plates/engravings)
Slice-k    PARTITION_OF        OCR
Goblin-k   OBSERVED            Slice-k
Finding-X  DERIVED_FROM        Slice-k
```

Provenance becomes **computational, not narrative**. This is Crystal Palace Invariance applied to
ingestion: *shared address space ≠ shared epistemic weight*.

## 3. The goblin is now typed — O ≠ H

Each goblin emits the same structured object, not a summary:

```
Gᵢ = ( Sᵢ , Oᵢ , Hᵢ , Fᵢ , Uᵢ , Nᵢ )
  Sᵢ  slice identity / window hash          Uᵢ  unresolved / OCR-ambiguous material (→ 𝒰)
  Oᵢ  direct source observations            Nᵢ  compost nutrients (weak signal, keep for later)
  Hᵢ  candidate chiddushim (HELEN could learn)   Fᵢ  falsifications of existing HELEN assumptions
```

The constitutional separation:

```
Oᵢ ≠ Hᵢ        "the catalogue structures X"  ≠  "HELEN could learn Y from X"
```

A goblin that collapses observation into interpretation has laundered the source. The loop rejects it.

## 4. N_eff is empirical, measured by Fable — not the goblin count

```
N_eff ≠ #Goblins
N_eff = #{ reproduced, structurally NON-equivalent mechanisms }
```

Fable receives `𝒢 = {G₁…G₁₀}` and clusters the `Hᵢ` into equivalence classes:

```
Hᵢ ~ Hⱼ  ⟺  same structural mechanism
```

**Swarm-1 already shows the collapse working.** Ten superficially different findings —
*name index · classified index · class number · exhibitor number · national section · gallery
position · cross-reference · page locator* — collapse into ONE deeper mechanism:

```
⟶  ADDRESSABILITY
```

That is the empirical seed of N_eff: eight surface "discoveries," one reproduced mechanism. The loop's
job over 20 epochs is to grow the count of **distinct** mechanisms, not the pile of restatements.

## 5. The deepest object is neither EXHIBIT nor VIEW — it is the ADDRESS

```
A_E = ( id_E , r_E , λ_E , π_E )
  id_E  content identity        λ_E  lineage
  r_E   representation root      π_E  placement / index coordinates
```

```
E may acquire arbitrarily many addresses / views without acquiring authority.
```

The 1851 catalogue is a Warren precursor: it made 100,000+ radically heterogeneous objects
**co-addressable** (name ∨ nation ∨ class ∨ page) without making them homogeneous, corroborated, or
authoritative. Each of Identity × Classification × Placement × Description × Provenance can change
independently:

```
Δ Description(E)  ⊬  Δ Identity(E)
```

That is directly the model for HELEN memory evolution — descriptions improve, identity holds.

## 6. Empirical signals confirmed by swarm-1 (real catalogue text, not conjecture)

- **30-class, 4-section master taxonomy**: I Raw Materials (1–4) · II Machinery (5–10) · III
  Manufactures (11–29) · IV Fine Arts (30). Classification is **material/process-first** until function
  overrides (Class 20 Clothing breaks the material grouping).
- **Catalogue as live document-machine**: type was set *before* the May 1 opening, then continuously
  rearranged as exhibitors moved items; foreign states' entries arrived a month late. The schema and
  the record of physical arrangement **co-evolve** — they are not separately authored.
- **Classes are containers, not quotas**: uneven pagination per class, no fixed allocation — open-ended
  population. (A Warren invariant: the address space is provisioned, the contents are not predicted.)
- **OCR degradation is uneven**: structural labels (class names, section headers) survive OCR; serial
  numbers and merchant names degrade → these become `Uᵢ` (𝒰), never silently "covered."

## 7. Constitutional boundary (unchanged, restated)

```
✨ interpretation
      ╳
      │
      ▼
   authority

📦 EXHIBIT ≠ 🪪 Π_D    🪪 Π_D ≠ ⚖ ADMIT    🗂 INDEX ≠ 👁 EVIDENCE    👁 VISIBLE ≠ 🌍 COMPLETE
```

## 8. Ruling carried into the loop

```
SOURCE OBJECT        : Volume I physically identified (of III)
PDF                  : 842 pages externally corroborated · direct extraction FAILED
OCR STREAM           : 163,936 lines (IA surrogate, lossy: text only)
OCR PARTITION        : 163,936 / 163,936 dispatched  (PartitionCoverage = 1)
SOURCE↔OCR FIDELITY  : NOT YET EARNED
FULL 3-VOLUME SCAN   : FALSE / NOT CLAIMED
N_eff                : measured by Fable per epoch (swarm-1 first collapse → ADDRESSABILITY)
AUTHORITY / CANON    : 0 / FALSE · LEDGER_EFFECT : NONE
```

The strongest chiddush, produced before the loop even runs:

```
Coverage must attach to a representation and an operation, not vaguely to "the source."
```

---

## 9. Feedback lock — representation-scoped coverage (V2.1)

Second Fable ruling accepted. Refinements over §1–§5:

**9a. Coverage is a 5-vector bound to (representation, operation, scope).**
```
C(R(E), Operation, Scope) = ( C_artifact , C_representation , C_extraction , C_dispatch , C_interpretation )
```
`C_representation` is now explicit (the scanned PDF and the OCR are two *observed* representations, before
extraction is even attempted). The governing law is sharper than §1:
```
Coverage(E)  is ILL-TYPED.        Only  Coverage(R(E), Operation, Scope)  is well-typed.
```
Only a proper transport witness could lift a representation-coverage up to a property of the exhibit E.

**9b. RepresentationOf is a two-hop chain, not one edge.** OCR is not derived from the historical concept
directly; it is derived from the *scan*:
```
🏛 E (Vol. I)  ──REPRESENTED_BY──▶  📕 R_scan  ──OCR_DERIVED_FROM──▶  🔤 R_OCR  ──PARTITIONED_INTO──▶  S₁…S₁₀
```
This admits future OCRs / transcriptions / editions of the same scan without identity collision.

**9c. The ADDRESS is `A = (E, R, λ, π)`.** A goblin never observes "the catalogue" — it observes an address:
```
Observation(Gᵢ, Aᵢ)      Aᵢ = ( E=CatalogueVolI , R=IA_OCR , λ=book→scan→OCR , π=lines[start:end] )
```
Finer form (upgrade): each `o ∈ Oᵢ` should carry `{slice_hash, line_start, line_end, excerpt_hash}`, not
reworded prose. The loop currently addresses at goblin/window granularity (`S = {slice, window, hash}`);
per-observation excerpt-hashing is the next fidelity step.

**9d. Memory object is five-part `E = (I, C, P, D, L)`** — Identity · Classification · Placement ·
Description · Lineage — and each descriptor mutates independently of identity:
```
ΔD(E) ⊬ ΔI(E)      ΔP(E) ⊬ ΔI(E)      ΔC(E) ⊬ ΔI(E)
```
Institutional memory evolves (better descriptions, reclassification, relocation) without rewriting the
identity of what was recorded.

**9e. N_eff is a quotient, not a count.**
```
N_eff = | M_reproduced / ∼_root |
```
Goblins 2, 5, 9 all finding "exhibitor numbering" through different OCR garbling = ONE class, not three
votes. One goblin may yield several genuinely distinct mechanisms. Agreement ⊬ evidence; reproduction of a
*root mechanism* is the unit.

**9f. Fable synthesis is 🟣 FABLE_INFERENCE, never an observation — and carries a falsification test.**
When Fable collapses {name index, class number, exhibitor number, gallery position, national section,
cross-reference} into `ADDRESSABILITY`, that grouping is a HYPOTHESIS, not something the catalogue asserted.
It is validated only when an independent C17 tracer **physically uses the ADDRESSABILITY syntax to retrieve
a known target** from the dataset. Until then:
```
👁 FABLE grouping = 🟣 generative projection ⊬ 🔵 observation ⊬ 🪪 Π_D ⊬ ⚖ ADMIT
```

**9g. The method-chiddush, restated as HELEN law:**
```
A historical object is not one representation, and coverage is never representation-invariant by default.
The Warren makes heterogeneous cognition CO-ADDRESSABLE without making it
homogeneous, corroborated, or authoritative.
```

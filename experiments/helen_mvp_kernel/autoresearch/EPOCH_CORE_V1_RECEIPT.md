# AUTORESEARCH EPOCH — RECEIPT (HELEN_CORE_V1 constitution projection)

🔵 OBSERVED · NON_SOVEREIGN · authority=0 · not admitted · no ledger effect
Loop: HELEN°FABLE supervision · operator directive "build HELEN_CORE_V1" @ 8a11fd1

## 7-field receipt

- **carry_forward_state**: an architectural verdict flagged the real risk as SPINE LOSS INSIDE
  VOCABULARY — stable law, candidate modules, historical recaps, and reported implementations all
  coexist in one large registry, so retrieval can surface obsolete/speculative structure as current.
  The recommended move was compaction, not another organ.
- **hypothesis**: the constitution can be projected to a small, typed, machine-checkable spine, and
  the "status is metadata, not structure" gap can be closed with an executable promotion gate.
- **experiment**: FIRST measured the corpus (derive-not-assert), then built helen_os/core/ —
  HELEN_CORE_V1.json (spine) + core_v1.py (typed promotion gate) + 11 fixtures.
- **metric**: does the corpus measurement replace the relayed figure, and do the seven declared
  promotion rules refuse status-laundering while a fully-earned claim still promotes (non-vacuity)?
- **result — BUILT, GREEN (229→240)**:
  - **corpus MEASURED**: docs/ = 78,437 lines · docs/proposals/ = 26,161 lines / 106 files · root
    constitutional = 1,385. The relayed "16,000 lines" was an UNDERESTIMATE; the surface is ~78k.
    The verdict holds *more* strongly than reported. (Encoded in the JSON, asserted in a test.)
  - **spine**: 8 modules (owns / must_not) · 12 invariants I01–I12 (the whole session's doctrine
    compressed) · canonical runtime (15 arrows) · minimal interfaces · evidence-status ladder.
  - **typed promotion gate** (core_v1.py): the 6-rung ladder HYPOTHESIS→REPORTED→FIXTURE_GREEN→
    FRAME_BOUND_PASS→TRANSPORTED→ADMITTED, and can_promote() fails closed on each launder path:
    REPORTED_NOT_PROVEN, MISSING_CONTRADICTION_HOLD, NO_WITNESS, STALE_FRAME, RENDER_NOT_ADMISSION,
    MEMORY_NOT_REPLAY. is_stable() denies candidates; is_live() denies stale RUNTIME reports.
  - **fixtures**: all seven declared rules green (candidate ⊬ stable, reported ⊬ proven, render ⊬
    admission, memory ⊬ replay, missing-contradiction → HOLD, missing-witness → no promotion, stale
    runtime ⊬ live) + a positive control (an earned NORMAL claim promotes) so the gate is non-vacuous.
- **keep/reject rule**: KEEP. This REDUCES surface: a ~78k-line doctrine becomes a one-screen typed
  spine whose status rules are executable. It is a projection (navigation), explicitly NOT canon and
  NOT a replacement for the full registry.
- **upgrade_path / RESIDUAL**: the spine is hand-extracted, not derived by a tool over the 78k lines —
  a claim could be omitted or mis-typed and the gate wouldn't know (the D⁻/coverage problem, one
  level up). Wiring an extractor that ingests the real registries and emits status-typed records is
  the production step. Per the frozen order, C17 (Π_D) still comes before that: the spine says WHAT
  must be typed; C17 is HOW coverage of the typing is honestly earned. No sovereign path touched.

## Fable supervision note
Operator directed the compaction build. Fable measured the corpus first (78k, not the relayed 16k),
then extracted the spine and made its status rules executable — the first tranche this session that
shrinks the architecture instead of extending it. The gate enforces I10/I11 directly: render ≠
authority, memory ≠ ledger, admission ≠ external truth, proposer ≠ validator ≠ sealer. Neither admitted.

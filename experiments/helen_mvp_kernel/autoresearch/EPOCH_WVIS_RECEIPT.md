# AUTORESEARCH EPOCH WVIS — RECEIPT (perceptual-transport membrane)

🔵 OBSERVED · NON_SOVEREIGN · authority=0 · not admitted · no ledger effect
Loop: HELEN°FABLE supervision · operator directive "build WVIS first" @ 8ffe0ff

## 7-field receipt

- **carry_forward_state**: the WVIS spec was discussed at length across relays, but the
  implementation (VisualNode/χ/legacy test) lived in ANOTHER frame — absent from this checkout
  (grep confirmed). Per discipline I built it FRESH here rather than editing a mirror, folding in
  the converged corrections: closed-authority-surface ≠ frozen-vocabulary, and the API-negative
  caveat on non-reversibility.
- **hypothesis**: a forward-only projection membrane π_V: X_machine → V_WUL can render (τ,φ,χ,ρ)
  while making authority STRUCTURALLY unrepresentable, and the five WVIS falsifiers can prove it.
- **experiment**: built helen_os/surface/wvis.py — VisualNode/VisualEdge (closed types), Phi,
  PHASE_COLOR/CHI_MARK (distinct channels), FORBIDDEN set, validate_ir_payload, project(),
  system_verdict(), render_antv(). 8 falsifiers.
- **metric**: does the surface reject/never-carry authority, preserve UNKNOWN, keep edges
  independent of nodes, expose no reverse path, and keep χ non-interfering with φ?
- **result — BUILT, GREEN (250→258)**:
  - **WVIS-01 schema-authority exclusion**: FORBIDDEN ∩ Fields = ∅ enforced by module-load asserts
    on both types (structural, not documented); validate_ir_payload REJECTS a smuggled authority
    coordinate (E_AUTHORITY_IN_SURFACE), never ignores. WVIS-01b: adding χ does NOT break the
    membrane (closed surface ≠ frozen vocabulary — the exact-shape-freeze failure mode avoided).
    WVIS-01c: project() DROPS authority/mint/commit — they exist in X_machine, never cross to V.
  - **WVIS-02 UNKNOWN preservation**: machine UNKNOWN → visual φ=UNKNOWN, carried by BOTH color
    (🟡) and redundant text ([UNKNOWN]); no missing-style/fallback path can read it as green.
  - **WVIS-03 edge independence**: two PASS nodes + a FAIL edge → system_verdict = FAIL, never
    PASS; an UNKNOWN edge → UNKNOWN. C14 (green nodes ⊬ green graph) made visual. Positive control
    WVIS-03b: all-green nodes AND edges → PASS (non-vacuity).
  - **WVIS-04 projection non-authority**: no from_svg/parse_svg/apply_syntax/to_machine_state/mutate
    on the module; render_antv returns a plain dict leaf with no callable and no back-pointer.
  - **WVIS-05 channel non-interference**: Δχ ⇒ Δφ=0 — changing χ flips the halo (◍¹→◍⁷) but the
    φ core color and text are unchanged. χ and φ are genuinely distinct channels.
- **keep/reject rule**: KEEP. The perceptual companion to C17: C17 protects witness transport
  across frames, WVIS protects governed-state transport into perception. Both are non-laundering
  membranes; neither promotes anything (render ⊬ admission, SVG ⊬ X_machine).
- **upgrade_path / RESIDUAL**: WVIS-04 is an API_NEGATIVE_CHECK — it proves TODAY's absence of a
  reverse path, NOT structural irreversibility. The stronger property (R: X→V exists ∧ V→X ∉ public
  API, provable) is later work. render_antv emits a node spec, not a full AntV document / SVG codec
  — wiring the actual AntV renderer + a closed-schema JSON validator (additionalProperties:false on
  the wire, not just the dataclass) is the production step. NON_SOVEREIGN, no sovereign path touched.

## Fable supervision note
"build WVIS first": the spec was another-frame, so Fable built it here — the projection membrane
where authority is unrepresentable by construction (module-load asserts + drop-on-project +
reject-on-smuggle), χ added as a legitimate channel without freezing the vocabulary, and the C14
non-collapse made visual (green nodes ⊬ green graph). Neither admitted. render ⊬ authority.
